# Status

## Current state

**Phase 6 Slice 6b landed (2026-04-15) — remote agent deploy pipeline + `tepegoz doctor --agents`.** Builds on 6a's wire v10 + embedded agents to actually push + run an agent on a remote host. `tepegoz-ssh::deploy` module exposes `detect_target` (parses `uname -sm` → Decision #3 target triple; macOS `arm64` normalized to `aarch64-apple-darwin` per rust's convention), `inspect_remote_agent` (presence + SHA256 + stat for `doctor --agents`), `deploy_agent` (raw-channel `cat > .tmp` + atomic `mv` + `chmod +x`, verify-by-hash with a single retry on mismatch — no `russh-sftp` dep; reuses the exec-channel pattern proven in ssh_smoke.rs), `spawn_agent_channel` (exec the deployed binary in a fresh channel, returns the channel for wire-protocol use), `handshake_agent` (writes `AgentHandshake` + reads `AgentHandshakeResponse` via the codec + asserts version match, terminal on mismatch per the brief — no retry on protocol drift). Remote hash command picked per-OS (`sha256sum -b` Linux / `shasum -a 256 -b` macOS). Remote path convention: `$HOME/.cache/tepegoz/agent-v<N>` where N is PROTOCOL_VERSION (resolved via `echo $HOME` over exec; tilde expansion across sshd configs is unreliable). **Agent TOFU model (CTO-aligned, pending sign-off)**: the embedded binary is the source of truth, every deploy verifies content-hash against it — no first-seen-wins stored-hash DB like SSH host keys. The cache-hit branch is idempotence, not trust. New `tepegoz doctor --agents` walks the Fleet host list and reports per-host deploy state (triple, remote present/absent/drift, sha256 prefix match); non-fatal on per-host connect errors. `cargo xtask demo-phase-6` gained `--remote` flag — spawns `tepegoz-demo-phase-6-sshd` container (separate from demo-phase-5's), cross-compiles agent via `cargo zigbuild` (if available) or plain `cargo build --target`, runs the full deploy + handshake round-trip. Universal macOS `lipo` deferred to Phase 10 release pipeline per the brief (requires Xcode / llvm-lipo tooling not guaranteed on dev/CI boxes). CI opt-in gate `TEPEGOZ_SSH_TEST=1 + TEPEGOZ_DOCKER_TEST=1` runs the full deploy + handshake integration test; `xtask build-agents` still excluded from CI per the 6a rule. Known limitation logged in ISSUES: protocol bumps leave orphaned `agent-v<N-1>` binaries on remote hosts — not a 6b blocker, Phase 6 close or v1.1 cleanup.

**Phase 6 Slice 6a landed (2026-04-15) — agent binary scaffolding + wire v10 + controller embedding + local handshake demo.** `tepegoz-agent` becomes a real stdio-framed protocol server with a reusable `lib::run_stdio()` entry point; the controller's `tepegoz agent --stdio` subcommand now routes through the same `run_stdio` so there's one implementation, two invocation shapes. **Wire protocol v9 → v10**: adds `Payload::AgentHandshake { request_id }` (controller → agent) + `Payload::AgentHandshakeResponse { request_id, version, os, arch, capabilities }` (agent → controller). Capabilities carried as strings (`"docker"` / `"ports"` / `"processes"` / `"pty"` as 6c/d populate) so adding a new probe kind doesn't require a wire bump. `PROTOCOL_VERSION` moved to a plain-text file at `crates/tepegoz-proto/PROTOCOL_VERSION`; `tepegoz-proto/build.rs` ingests and emits the Rust const to `OUT_DIR` — same text file is read by `xtask::build_agents` (for manifests) and `crates/tepegoz/build.rs` (for the drift check). **Version-drift defence (load-bearing per the brief)**: two lines. (1) Controller `build.rs` — for each of the 4 target triples, if `target/agents/<triple>/{tepegoz-agent, manifest.json}` exists, `include_bytes!` the binary + parse manifest + `panic!` if `protocol_version` differs from the proto text file. Mismatch is a hard compile failure; a stale agent can never masquerade as current. (2) Runtime handshake — agent echoes its compiled-in version in `AgentHandshakeResponse`; controller asserts match before subscription dispatch. Agent also rejects inbound envelopes whose `version` field doesn't match its own, returning `ErrorKind::VersionMismatch` before closing. **Graceful fallback**: plain `cargo build` (without `cargo xtask build-agents` having run) emits one `cargo:warning` and populates every `embedded_agents::<ARCH>` slot with `None`. CI's existing workspace build is untouched — the brief explicitly said `xtask build-agents` is a local + release-pipeline tool, not a CI gate. **xtask additions**: `cargo xtask build-agents` (cross-compiles all 4 targets via `cargo zigbuild --profile release-agent`, copies to `target/agents/<triple>/tepegoz-agent`, writes `manifest.json` sidecar with `protocol_version` + `target_triple` + `built_at_unix_secs`), and `cargo xtask demo-phase-6 up/down` (plain-cargo host-target build + subprocess handshake + pretty-printed response; `down` removes the demo root tempdir). Universal macOS `lipo` deferred to 6b. CI stays green because the `target/agents/` tree is never populated there. **Non-scope for 6a (deferred to 6b)**: SSH deploy (scp + SHA256 verify + remote exec), agent TOFU, `tepegoz doctor --agents`; 6c/d will add real probes (docker / ports / processes / pty) populating the capabilities list. **Slice 6.0 + 6.0.1 landed (2026-04-15, pre-Phase-6) — mouse-first UX + keybind simplification + help overlay + Tab-in-PTY carve-out.** Keyboard surface reduced to five documented bindings (`Tab` / `Shift-Tab` tile focus, arrows / `j` / `k` row nav, `Enter` primary action, `Esc` cancel/back, `Ctrl-b d` detach) + `Ctrl-b ?` help overlay. `Ctrl-b 1..9`, `Ctrl-b 0`, `Ctrl-b n`, `Ctrl-b p`, `Ctrl-b w`, `Ctrl-b q`, `Ctrl-b Enter` removed entirely; `Ctrl-b h/j/k/l` + `Ctrl-b &` retained as undocumented power-user aliases (handlers stay, help-bar + docs drop the mention). Mouse support: crossterm `EnableMouseCapture` in `terminal::enter_raw`; SGR sequence parser (`tepegoz_tui::mouse::MouseParser`) strips `\x1b[<Cb;Cx;CyM/m` from the stdin byte stream before `InputFilter` sees it, emits `AppEvent::MouseClick { x, y }` + `AppEvent::MouseHover { x, y }` on the existing event bus. Per-tile hit-testing: tile border click → focus; PTY tab-strip click → switch active pane; new `[×]` close affordance at strip tail → close active pane (auto-reopens local root if stack would empty); scope body row click → focus + select; double-click within 400 ms window → primary action (Fleet → open remote pane; Docker/Ports no primary action today). Hover visual: unfocused-hovered tiles render `Color::Cyan + Modifier::DIM` border via new shared `scope::border_style(focused, hovered)`, sitting between focused-bright-cyan and idle-dim-gray. Help overlay: centered modal listing the 5-binding surface + per-tile intra-keybinds; any keypress or click dismisses (except `Ctrl-b d` which still detaches as escape hatch). Decision #7 amended with the Input/interaction spec + Mouse bus. **Slice 6.0.1 (same-day carve-out)**: `App::handle_tab` forwards `\t` / `\x1b[Z` to the active pane as SendInput when the PTY tile is focused, so shell tab-completion + any pty app that consumes Tab work as on a non-tepegoz terminal; non-PTY tiles still get Tab/Shift-Tab cycling. Decision #7's Input/interaction section carries the carve-out wording; the original 6.0 "Tab never reaches the pty shell" ISSUES entry moved to the Resolved archive with a 6.0.1 commit pointer. Wire protocol unchanged (v9). Slice-level verification only — render tests for hover + click routing, state-machine tests driving synthesized `AppEvent::MouseClick`/`MouseHover` + an end-to-end round-trip through `MouseParser` → App + explicit PTY-focused Tab/Shift-Tab pins. No `cargo xtask demo-slice-6-0` per the "xtask rule kicks in at Phase 6" scope call. **Known limitation** (still in `docs/ISSUES.md`): panes past slot 9 are not mouse-reachable — the `[+N]` overflow indicator renders but isn't clickable today, and the keyboard-only `Ctrl-b 0` that previously reached the 10th slot is gone. Close earlier panes to shift hidden ones into view; Phase 6 agent-based multiplexing revisits the overflow overlay.

**Phase 5 closed (2026-04-15) — SSH transport + remote pty + Fleet supervisor + multi-pane TUI ship end-to-end.** Verified via the user's manual demo of scenarios 1-4 (CLI connect, multi-pane Fleet open, `Ctrl-b 1/2/n/p` switching, detach/reattach) via `cargo xtask demo-phase-5 up`; scenarios 5-8 (SSH drop, reconnect UX, auth fail, host-key mismatch) were NOT manually walked — user's explicit "move on, fix if reports surface" priority call. Each underlying piece of the failure-mode machinery is machine-tested at the layer level (daemon supervisor path pinned by opt-in `fleet_scope.rs`; `known_hosts` / `session` / `scope::fleet` unit tests cover scenarios 7-8 pieces); the end-to-end "real SSH failure → red toast renders" path is the documented coverage gap, logged in `docs/ISSUES.md` under "Phase 5 close caveats (manual demo coverage gap)" with the diagnostic path if bugs surface later. **New engineering-quality standing rule**: every phase with a manual-demo gate ships a `cargo xtask demo-<phase>` runner that does all fixture setup in one shot — Phase 5 got this retroactively in `e51cf35` + `b226c03`; Phase 6 Slice 6a onward ships demo tooling alongside the first meaningful implementation, not deferred. **Wire protocol v4 → v9** across Phase 5: v7 for Fleet subscriptions (5b), v8 for `FleetAction` + `HostStateChanged.reason` (5c-ii), v9 for `OpenPaneSpec.target: PaneTarget::{Local, Remote { alias }}` (5d-i). **Daemon side**: `tepegoz-ssh` concrete crate (russh 0.60 + ring backend, TOFU against tepegoz-owned `known_hosts`, SSH_AUTH_SOCK + IdentityFile auth chain with verbatim russh reason propagation, `~/.ssh/config` parsed via russh-config with Include-directive mitigation); per-host `host_supervisor` with lazy-connect + `autoconnect` opt-in + 1/2/5/15/60 s backoff + 30 s keepalive heartbeat + Degraded on single miss + Disconnected on 3 misses + autoconnect-age reset at 30 s; `RemotePaneManager` mirrors `tepegoz_pty::PtyManager` for the `subscribe`/`send_input`/`resize`/`close` surface with an identical `PaneUpdate` broadcast shape so `forward_pane` and `forward_remote_pane` both route through `handle_command`'s pane-ops without the client knowing local-vs-remote; disjoint `PaneId` namespaces (local 1+, remote 1<<32+) caught by a `debug_assert!` tripwire. **Client side**: `ScopeKind::Fleet` tile renders the four-glyph state set per Q6 (`●` Connected / `◐` Connecting+Degraded / `○` Disconnected / `⚠` AuthFailed+HostKeyMismatch), four-state-lifecycle + filter + selection + `Ctrl-b r` reconnect + red toast on transition-into-terminal; pane-stack + tab strip inside the PTY tile (`[N label*]` format, `*` suffix on active, `Modifier::DIM` inactive, `[+N]` overflow beyond nine slots) with `Ctrl-b 0..9/n/p/&/Enter` keybinds; `tepegoz connect <alias>` CLI subcommand opens a fresh remote pane as the sole stack entry for one-shot "drop me into a remote shell" use. Manual demo caveats carry forward into Slice 6.0's verification envelope — the TUI rendering layer Slice 6.0 touches (mouse capture + hover states + help overlay) would likely surface any regression in the un-walked scenarios. Implementation-quality: **reference-tier 5a/5b execution** per the CTO HANDOFF (5a landed every directive exactly, 5b caught a real russh-config Include bug + recovered from the Linux→darwin cross-build failure by switching to the ring backend + landed `--ssh-forget` with the right "touch only what we wrote" security posture); **Phase 4 desync lesson preemptively applied** in 5c-ii (engineer spawned the russh connect future on a oneshot rather than select-dropping it because `russh::client::connect`'s cancellation safety is undocumented); **engineering-quality wall hit + fixed** mid-5e when the original multi-terminal demo script proved unrunnable in practice, leading to the xtask rule.

**Phase 4 closed (2026-04-14) — Ports + Processes panels ship end-to-end.** Verified via the user's 8-scenario manual demo sign-off against a real terminal. Daemon side: `Subscribe(Ports)` and `Subscribe(Processes)` stream via a cross-OS probe abstraction (netstat2 + sysinfo on both OSes, procfs/cgroup on Linux, libproc on macOS) using the uniform `HashMap<id, AbortHandle>` subscription pattern Phase 3 established; daemon-side port→process→container correlation (cgroup-derived on Linux, `HostConfig.PortBindings` lookup on macOS) so clients stay dumb; `Event::PortList`/`PortsUnavailable` (protocol v5) and `Event::ProcessList`/`ProcessesUnavailable` (protocol v6) with `cpu_percent: Option<f32>` preserving the "first-sample-not-yet-measured" ≠ "idle / 0.0%" distinction across rkyv. Client side: `ScopeKind::Ports` tile wraps two coequal views (`PortsView` + `ProcessesView`), each with its own three-state lifecycle (Connecting / Available / Unavailable), filter, and selection; `p` toggles Ports ↔ Processes (absorbed during filter input); selection persists across refreshes by stable keys — `(protocol, local_port, pid)` for Ports and `(pid, start_time_unix_secs)` for Processes — pid reuse never silently retargets. Processes CPU% renders `None` as em-dash (`—`), `Some(x)` as a number; Ports footer shows `UDP coming v1.1` per the 4c UDP-defer resolution. **Wire desync root cause + fix** (`77aa9ca`): `tokio::AsyncReadExt::read_exact` in a `tokio::select!` is not cancellation-safe — under concurrent stdin/tick pressure against large (~50–100 KiB) ProcessList envelopes, a firing tick or stdin branch silently corrupted the TUI reader. Fix: dedicated `spawn_reader_task` mirroring the daemon's writer pattern, funneling envelopes through a cancellation-safe mpsc. Regression pinned by the default-suite `tepegoz-tui::session::tests::stdin_pressure_does_not_desync_large_envelopes`; production repro (`yes | tui`) confirmed clean over a 20 s alarm window on the fixed binary. User flagged one polish item during the demo: the `h/j/k/l` focus-nav keybinds move both horizontally AND vertically, not just vertically, and discoverability needs the reserved `Ctrl-b ?` help overlay wired up — tracked below as a Phase 4 polish candidate. Slice D (`DockerExec` → new pty pane) remains deferred to v1.1 pending Phase 5's "scope → new pane" mechanism crystallizing.

**Phase 3 delivered (closed 2026-04-14 via the user's 9-scenario manual demo).** Daemon side: `Subscribe(Docker)` streams live container lists with socket-discovery fallback across Docker Desktop / Colima / Rancher Desktop / native Linux, reconciling engine-availability transitions with one `DockerUnavailable` event per flip; `DockerAction` drives start/stop/restart/kill/remove with structured `DockerActionResult { request_id, outcome: Success | Failure { reason } }`; `Subscribe(DockerLogs)` and `Subscribe(DockerStats)` stream through a uniform `DockerStreamEnded { reason }` termination. All subscription kinds share one `HashMap<id, AbortHandle>` shape — Phases 4/5/7 inherit the mechanism. Client side: the tiled god view per Decision #7 renders PTY tile (top full-width) + Docker tile + Ports / SSH Fleet / Claude Code placeholders concurrently on a 120×40+ terminal, with tiny-terminal fallback below 80×24; `Ctrl-b h/j/k/l` + arrows move focus between adjacent tiles with spatial-adjacency + left-align tiebreak; the pty tile renders via `vt100::Parser` → ratatui cells (no more raw passthrough). Docker tile carries the three-state lifecycle (Connecting / Available / Unavailable), container table with navigation + filter, action keybinds (`r`/`s` immediate + `K`/`X` inline confirm modal with K→K absorption + 10 s auto-cancel + focus-away cancel), pending-action 30 s timeout sweep on every Tick, toast overlay (bounded stack of 3 directly above the Claude Code tile, per-kind auto-dismiss, drop-oldest on overflow), and a logs sub-state (`l` opens, `Esc`/`q` exits; scroll + auto-follow + `DockerStreamEnded` marker; `MAX_LOG_LINES = 10_000` drop-oldest ring; per-stream stdout/stderr pending buffers; CRLF stripped). Wire protocol v4. 165 tests green on both macOS and ubuntu-latest. Scope renderers take `(state, Frame, Rect, focused)` per the ARCHITECTURE §9 contract; Phase 4/5/7/9 panels drop in at their placeholder tiles as they land.

Phase 2 closed cleanly: the "immediate-detach" report turned out to be user confusion (attached pane shell is visually identical to the outer shell) — see `docs/ISSUES.md` resolved entry. TUI paints an OSC 0 window title (`tepegoz · pane N`) on attach so the pane is unmistakable.

## Phase matrix

| # | Name | Status | Commit(s) | Acceptance test(s) |
|---|---|---|---|---|
| 0 | Scaffold | ✅ | `81c7731` | `tepegoz --help`, green CI |
| 1 | Proto + daemon + TUI round-trip | ✅ | `3715bf9` | `daemon_persistence.rs` |
| 2 | Local pty multiplex + persistence | ✅ | `eab274c`, `321ed5e` | `pty_persistence.rs`, `subscribe_does_not_duplicate_bytes`, `pane_honors_cwd_and_exposes_pane_id_env` |
| 3 | Docker scope panel | ✅ (2026-04-14) | `24dc244`, `816765b`, `e4d2113`, `664703f`, `43b28eb`, `56a8a4f`, `c7c0d39`, `a5276cc`, `2c54c44`, `c7b336d`, `8a9176c`, `fc5ded4`, `4dd1208`, _close commit_ | 165 tests (130 TUI lib + 7 docker crate + 11 proto codec + 4 pty + 2 daemon_persistence + 8 docker_scope + 1 pty_persistence + 1 pane_unsubscribe + 1 vim_preservation/vt100-reconstruction) |
| 4 | Ports + processes panels (local) | ✅ (2026-04-14) | 4a: `1111bbf`, `8285543`, `4ba452e`; 4b: `d626f4f`; 4c: `4b850c1`; 4d: `d202edf`, `bee6aba`, `77aa9ca`, _close commit_ | 211 tests on macOS / 220 on ubuntu-latest (9 Linux-only cgroup parser tests) — 4d adds `ports_processes_e2e.rs` with 2 opt-in tests (combined Ports+Processes spawn/kill round-trip + opt-in-on-both-envs Docker-bound-port container correlation); post-4d desync fix adds `tepegoz-tui::session::stdin_pressure_does_not_desync_large_envelopes` default-suite regression pin + `desync_repro.rs` opt-in exploratory harness |
| 5 | SSH transport + remote pty | ✅ (2026-04-15) | 5a: `2b54d2e`; 5b: `f038cee`, `54d1c29`, `f255681`; 5c-i: `fd0e50e`; 5c-ii: `7120c44`; 5d-i: `e2f1b06`; 5d-ii: `35e7fd9`; 5e: `edbf360`, `da85cb8`, `e51cf35`, `b226c03`, _close commit_ | 281 tests on macOS / 290 on ubuntu-latest — 5d adds `pane_stack.rs` (2-pane byte-isolation); 5c-i/5c-ii add `fleet_scope.rs` opt-in (autoconnect + container kill + reconnect dispatch + unknown-alias failure); 5d-i adds `remote_pane.rs` opt-in (OpenPane-Remote full pipeline against openssh-server); `cargo xtask demo-phase-5 up/down` fixture runner (`e51cf35` + macOS preflight fix `b226c03`) |
| 5.5 | Slice 6.0 — mouse + keybind simplification + help overlay + 6.0.1 Tab-in-PTY carve-out | ✅ (2026-04-15) | _slice-6.0 commit_, _slice-6.0.1 carve-out commit_ | 332 tests on macOS / 341 on ubuntu-latest — adds `mouse.rs` (SGR parser, 12 tests), `help.rs` (overlay renderer, 3 tests), `scope::mod::tests` (border-style picker, 4 tests), `pty_tile::hit_test_tab_strip` + `[×]` render tests (5 new), `tile::cycle_focus` forward/backward/too-small tests (3 new), `input.rs` Tab/Shift-Tab/bare-Esc/removed-binding tests (9 new), `app` mouse-click/hover + Tab-cycle + help-overlay + 6.0.1 PTY-focused-Tab-forwards-to-pty state-machine tests (15 new) |
| 6 | Agent binary + remote scopes | 🟠 (6a 2026-04-15, 6b 2026-04-15, 6c-i + 6c-ii + 6c-iii + 6d-i + 6d-ii 2026-04-15) | 6a: _slice-6a commit_; 6b: _slice-6b commit_; 6c-i: `0531462`; 6c-ii: `16ca267`; 6c-iii: `0f6061d`; 6d-i: `f3840d7`; 6d-ii: _slice-6d-ii commit_ | 398 tests on macOS — 6a + 6b as previously; 6c-i: `tepegoz-proto::codec` +8 codec roundtrips via `subscription_roundtrip` helper closure covering `Local` + `Remote { alias: "prod-box" }` pairs for each retargeted Subscription variant; 6 existing tests updated for the new `target` field. 6c-ii: `tepegoz-agent::lib::tests` +5 (handshake + capabilities, ping, version-mismatch, unhandled-payload, unsubscribe-unknown-id-noop) — replaces 6a's handshake-only tests with subscription-capable shape; `tepegoz-core::agent::tests` +3 (alloc_id monotonic, shutdown notifies per-scope-kind with Docker → DockerUnavailable / Logs+Stats → DockerStreamEnded / Action → Failure result + idempotent 2nd shutdown, end-to-end duplex-streams test running real `tepegoz_agent::serve` in-process against daemon routing). 6c-iii: `tepegoz-tui::host_picker::tests` +5 (Local-first ordering, disconnected-host annotation, connected-host ●/connected marker, selected-row ▶, tiny-terminal fallback); `tepegoz-tui::app::tests` +12 (Ctrl-b t opens on Docker + preselects current target; Ctrl-b t on non-target-capable tile is silent no-op; Escape dismisses without commit; j/k + arrows + clamp navigation; Enter on connected host commits retarget with Unsub + Sub envelopes and resets state; Enter on greyed-out host is noop + picker stays open; click on Docker title bar opens picker; click while picker visible dismisses; Ctrl-b t while open dismisses; Ctrl-b d always detaches; same-target commit is noop; retarget from Logs view exits + unsubscribes both subs); `crates/tepegoz-core/tests/remote_docker_subscription_roundtrip.rs` +1 opt-in on `TEPEGOZ_SSH_TEST=1 + TEPEGOZ_DOCKER_TEST=1` (spawns sshd container with autoconnect=true, cross-builds agent, boots daemon with `run_daemon_with_resolver`, subscribes Fleet → waits for HostStateChanged(Connected) → subscribes Docker remote → asserts ContainerList or DockerUnavailable arrives on the client sub id → unsubscribes + verifies daemon doesn't crash) |
| 7 | Port scanner | ⚪ | — | — |
| 8 | Recording + replay | ⚪ | — | — |
| 9 | Claude Code pane awareness | ⚪ | — | — |
| 10 | QUIC hot path + release 0.1.0 | ⚪ | — | — |

Status key: ✅ complete · 🟡 code+tests green, user acceptance pending · 🟠 in progress · ⚪ not started.

## What works end-to-end

- Daemon binds user-scoped Unix socket at `$XDG_RUNTIME_DIR/tepegoz-$uid/daemon.sock` (fallback `$TMPDIR` or `/tmp`). Parent dir `0700` when default path (we own it), socket `0600`. Override paths leave parent perms alone.
- Stale-socket eviction on startup. Refuses to start under another live daemon. Graceful SIGINT shutdown with socket cleanup.
- Wire protocol v4: rkyv-archived `Envelope { version, payload }`, 4-byte big-endian length prefix, bytecheck validation on every read. Messages:
  - Commands: `Hello`, `Ping`, `Subscribe(Status | Docker | DockerLogs | DockerStats)`, `Unsubscribe`, `OpenPane`, `AttachPane`, `ClosePane`, `ListPanes`, `SendInput`, `ResizePane`, `DockerAction`
  - Responses: `Welcome`, `Pong`, `PaneOpened`, `PaneList`, `DockerActionResult`, `Error`
  - Events (in `Event(EventFrame)`): `Status`, `PaneSnapshot`, `PaneOutput`, `PaneExit`, `PaneLagged`, `ContainerList`, `DockerUnavailable`, `ContainerLog`, `ContainerStats`, `DockerStreamEnded`
- PTY manager owns panes; per-pane 2 MiB ring buffer (`VecDeque<Bytes>` with total-byte accounting, oldest-chunks-evict on overflow); per-pane reader + waiter threads; `tokio::sync::broadcast` channel for subscribers.
- Docker subscription: per-`Subscribe(Docker)` task walks platform socket candidates (`$DOCKER_HOST` env > Docker Desktop > Colima > Rancher Desktop > native Linux), pings the first reachable engine, and emits a `ContainerList` immediately plus every 2 s. On unreachable engine: emits `DockerUnavailable { reason }` once per availability transition, retries `Engine::connect` every 5 s. Survives `dockerd` restarts; survives the user starting docker after subscribing.
- Docker lifecycle actions: `Payload::DockerAction { request_id, container_id, kind }` runs in a spawned task so a slow daemon doesn't stall the session loop. Always replies with `DockerActionResult { request_id, container_id, kind, outcome }` — engine-unavailable and bollard errors both surface as `Failure { reason }` (verbatim from dockerd; daemon doesn't try to classify). `Remove` is force-remove (`docker rm -f` semantics).
- Docker logs streaming: per-`Subscribe(DockerLogs)` task opens a bollard log stream (stdout + stderr) and forwards each chunk as `Event::ContainerLog { stream, data }`. Always emits a terminal `Event::DockerStreamEnded { reason }` — engine unreachable, container exit, container removal — so a UI knows the stream is done and never spins waiting for chunks that won't come.
- Docker stats: per-`Subscribe(DockerStats)` task streams bollard stats (~1/sec). CPU% is computed from cpu/precpu deltas using the standard docker-stats-CLI formula; `0.0` whenever the calculation can't be performed (first sample, missing precpu on Windows, sys_delta=0). Like logs, terminates with `DockerStreamEnded`.
- Daemon client session: single dedicated writer task drains an mpsc of outgoing envelopes — no per-write locks. **Subscription uniformity**: pane and docker subscriptions share the same `HashMap<id, AbortHandle>` shape (`pane_subs` and `docker_subs`). `Unsubscribe { id }` cancels across both maps with a single check. Phase 4 (Ports/Processes), 5 (SSH remote pty), 7 (port scanner) inherit this shape — every new subscription kind adds a map alongside, not a new mechanism. (Through Slice C1 `pane_subs` was a `JoinSet<()>` with no per-id key, so `Unsubscribe` of a pane sub silently no-op'd; the C1 TUI's synthetic re-attach was leaking one zombie forwarder per Scope→Pane mode switch. Fixed in the C2-gate commit `43b28eb`; regression test `pane_unsubscribe.rs` pins the invariant.)
- TUI architecture: pure-state-machine `App` (in `tepegoz-tui/src/app.rs`) handles every external happening as an `AppEvent` (StdinChunk, DaemonEnvelope, Resize, Tick, PendingActionTimeout) and emits `AppAction`s (SendEnvelope, DrawFrame, FocusTile, Detach{User|PaneExited}, ShowToast{kind,message}). The `AppRuntime` (in `tepegoz-tui/src/session.rs`) executes those actions against the daemon socket, stdin/stdout, and ratatui's terminal. State-machine tests live in `app::tests`; headless render tests in `scope::docker::tests`, `scope::placeholder::tests`, `pty_tile::tests`, and `tile::tests`.
- TUI view model (Decision #7): fixed tiled god view, no mode switching. `View { layout: TileLayout, focused: TileId }`. Five tiles on a 120×40+ terminal: PTY (top full-width), Docker (bottom-left), Ports placeholder "Phase 4" (bottom-middle), SSH Fleet placeholder "Phase 5" (bottom-right), Claude Code placeholder "Phase 9" (bottom-full-width strip). Below 80×24 the layout collapses to a single "Terminal too small" fallback tile. All subscriptions (`AttachPane`, `Subscribe(Docker)`, later Ports/Fleet/ClaudeCode) live concurrently for the life of the TUI.
- TUI focus navigation (Slice 6.0): `Tab` cycles forward through the fixed tile order (PTY → Docker → Ports → Fleet → ClaudeCode → PTY), `Shift-Tab` cycles backward. Mouse click on any tile focuses it. `Ctrl-b h/j/k/l` + `Ctrl-b` + arrow keys remain as undocumented power-user aliases for spatial (vs. cyclic) focus movement (spatial adjacency with left-align tiebreak, so `Ctrl-b j` from full-width PTY lands on Docker rather than Ports). `Ctrl-b d` detaches from any tile; `Ctrl-b q` was removed in Slice 6.0. The focused tile owns the keystroke stream; unfocused tiles continue to update live. Input routing per tile kind: Pty → `SendInput`; Scope(Docker) → scope key parser (j/k/arrows/g/G/Home/End/`/`+filter); Scope(Fleet) → scope key parser + plain `Enter` → open remote pane for selected host; Placeholder/TooSmall → drop. Placeholder tiles show a "Phase N — not yet implemented" hint when focused. `Ctrl-b ?` toggles the help overlay, which is the authoritative reference for the keyboard surface.
- Pty tile rendering: pty bytes from `Event::PaneOutput` / `Event::PaneSnapshot` feed a `vt100::Parser` owned by the App. The pty tile renderer projects the parser's screen buffer (cells + fg/bg colors + bold/italic/underline/reverse attributes) into ratatui cells within the tile's `Rect`. Cursor rendered as a reversed cell only when focused (unfocused tiles show the buffer without a misleading caret). Parser is resized on every Resize event to match the pty tile's new dimensions; corresponding `ResizePane` envelope sizes the daemon-side pane to match the tile (NOT the full terminal), so vim et al. render inside the box.
- Scope rendering contract (ARCHITECTURE §9; amended Slice 6.0): every scope renderer takes `(state, Frame, Rect, focused: bool, hovered: bool)` and draws only within its `Rect`. Border style is picked by the shared `scope::border_style(focused, hovered)`: focused → bright cyan no dim; unfocused + hovered → cyan + DIM; idle → dark gray + DIM (focus wins over hover when both are set). `scope::docker::render` preserves all C2c2 rendering logic (three-state lifecycle: Connecting/Available/Unavailable, container table with `▶` selection marker, filter bar with `_` caret when active, help bar). `scope::placeholder::render` draws a bordered block with centered label + focused hint. Phases 4/5/7/9 inherit — their tile slots into the god view at its placeholder and is replaced by the live renderer when the phase lands.
- TUI action keybinds (C3a): focused Docker tile recognizes `r`/`R` (Restart) and `s`/`S` (Stop) for immediate dispatch, plus `K` (Kill) and `X` (Remove) which first enter an inline confirm modal per CTO §4. Dispatch allocates a request id via the shared monotonic allocator, inserts a `PendingAction { deadline: now + 30 s, description }` keyed by that id, and sends `Payload::DockerAction(DockerActionRequest { request_id, container_id, kind })`. Keybinds no-op when Docker is Unavailable or the visible list is empty; focus on a non-Docker tile routes the keys to the normal input path (PTY's `SendInput`, placeholder drop).
- TUI confirm modal (C3a, UX clarification #3): centered bordered box rendered *inside* the Docker tile's `Rect` — never full-screen, other tiles keep rendering and receiving live updates throughout. Prompt text: "`<Kill|Remove>` container `<name>`? [y] confirm · any other key cancels". `y`/`Y` clears the confirm and dispatches the `DockerAction`; any other key clears the confirm without dispatching; focus moving away from the Docker tile cancels; a 10 s idle deadline clears the confirm on the next Tick sweep. Help bar swaps to the confirm-specific hint while active.
- TUI toast overlay (C3a, UX clarification #2): rendered as a 1-line-per-toast strip positioned directly above the Claude Code tile (full scope-row width). `App::toasts: VecDeque<Toast>` is bounded to 3 — a 4th arrival `pop_front`s the oldest silently so the user's keystrokes are never gated on a toast. Per-kind auto-dismiss: Success ~3 s, Error ~8 s, Info ~4 s. Toasts are `Success`/`Error`-colored content with a short `ok:` / `err:` / `info:` prefix. The 30 Hz Tick drives the sweep that retires expired entries; state transitions that want to surface a toast call `App::push_toast(kind, message, &mut actions)` which bounds the queue, appends `AppAction::ShowToast { kind, message }` for the runtime's `tui.log` trace, and emits `DrawFrame`.
- TUI pending-action lifecycle (C3a): on every 30 Hz Tick, `App::sweep_expired(Instant::now(), …)` walks `pending_actions` and drops entries whose `deadline` has passed, each expiring entry emits an "`<verb> <name>` timed out — check engine" Error toast. `DockerActionResult { request_id, kind, outcome }` looks up (and removes) the entry by `request_id`: `Success` → green "`<verb> <name>` — succeeded" toast; `Failure { reason }` → red "`<verb> <name>` failed: `<reason>`" toast (reason verbatim from dockerd); stale results (no matching pending action) fall back to `<verb> <container_id>` so the user still sees the outcome. `Payload::Error` from the daemon lands in the same overlay queue. `AppEvent::PendingActionTimeout(u64)` is retained on the event surface for a future dedicated timer sweeper without reshaping the API.
- TUI logs sub-state (C3b): `DockerScope.view` is a `DockerView::{List, Logs(LogsView)}` enum. `l` on the focused Docker tile allocates a sub id, sends `Subscribe(DockerLogs { id, container_id, follow: true, tail_lines: 0 })`, and transitions to `DockerView::Logs(LogsView { container_id, container_name, sub_id, lines: VecDeque, pending_stdout/_stderr: Vec<u8>, scroll_offset, at_tail, stream_ended })`. Events arriving on `LogsView.sub_id` are routed to `handle_logs_event` via `DockerScope::is_current_logs_sub`; `ContainerLog { stream, data }` appends bytes to the per-stream pending buffer and flushes every complete `\n`-terminated line (CRLF also stripped) into the capped ring (`MAX_LOG_LINES = 10_000`, drop-oldest). `DockerStreamEnded { reason }` flushes any trailing pending bytes as a final line, records the reason on `stream_ended`, and disables `at_tail`; the renderer paints a dimmed "— log stream ended: `<reason>` —" line at the tail. Scroll keybinds: `j`/`k`/Down/Up by one line; PgUp/PgDn by `LOGS_PAGE_LINES = 10`; `G`/End/Bottom jump to tail + re-enable auto-follow; `Esc` / `q` unsubscribe and return to List. Scrolling up sets `at_tail = false`; reaching offset 0 via scroll-down re-enables it. Logs view persists across focus moves; action keybinds (`r`/`s`/`K`/`X`/`l`/filter) are all ignored while logs are showing (read-only transcript). Stale events after Unsubscribe drop silently.
- TUI redraw cadence: 30 Hz `tokio::time::interval` always-on (no mode gating — the tile grid is always live). App responds to every `Tick` with `AppAction::DrawFrame`; the runtime draws the entire tile grid via ratatui's buffer diff (redundant-cell writes are cheap). State changes (pty output, container list update, focus move, filter edit) also emit `DrawFrame` directly so updates don't wait for the next tick.
- Daemon handshake: rejects protocol-version mismatches at the socket level. Both `Envelope.version` and `Hello.client_version` must equal the daemon's `PROTOCOL_VERSION`; otherwise the daemon sends a structured `Error(VersionMismatch)` naming both versions and closes. Without this guard a v3 client connecting to a v4 daemon would silently handshake and later trip an opaque rkyv decode error.
- Panes inherit `TEPEGOZ_PANE_ID=<id>` in env. TUI refuses to run if its own env has that var (prevents recursive attach feedback loop).
- Shells spawn in the TUI's `current_dir()` rather than `$HOME` (portable-pty's default).
- TUI sets the terminal window title to `tepegoz · pane N` on attach (OSC 0) and clears it on detach, so an attached pane is visually distinct from the outer shell.

## Test coverage (358 tests on macOS / 367 on ubuntu-latest, all green)

Phase 6 Slice 6b additions: `tepegoz-ssh::deploy::tests` +16 (uname-sm parser covering all four Decision #3 target triples including macOS `arm64` → `aarch64-apple-darwin` normalization + unsupported-os/arch error shape; hash-command selection per-OS; `parse_hash_output` acceptance of both sha256sum `<digest>  path` and shasum `<digest> *path` formats + rejection of non-hex + wrong-length tokens; `parse_stat_output` splits size/mtime; `shell_quote` roundtrips simple paths + escapes embedded single quotes via POSIX `'\''` idiom). `crates/tepegoz-ssh/tests/remote_agent_deploy.rs` +1 opt-in — gated on `TEPEGOZ_SSH_TEST=1 + TEPEGOZ_DOCKER_TEST=1`; spawns openssh-server container + cross-builds agent for x86_64-unknown-linux-musl + exercises connect / deploy / cache-hit-on-second-call / spawn / handshake end-to-end against a live remote.

Phase 6 Slice 6a additions: `tepegoz-proto::codec` +3 (`agent_handshake_roundtrip_preserves_request_id` + `agent_handshake_response_roundtrip_preserves_all_fields` + `agent_handshake_response_roundtrip_preserves_empty_capabilities`); `tepegoz-agent::lib::tests` +5 (`handshake_dispatch_echoes_request_id_and_reports_self` + `ping_dispatch_returns_pong` + `unhandled_payload_surfaces_invalid_request_not_silence` + `serve_handles_handshake_and_exits_on_eof` + `serve_rejects_version_mismatch_and_closes`); `crates/tepegoz-agent/tests/agent_handshake.rs` +1 (spawns `CARGO_BIN_EXE_tepegoz-agent` subprocess, drives a wire-v10 handshake through piped stdio, asserts request_id / version / os / arch / empty-capabilities, confirms clean EOF exit). No new Linux-only tests in 6a or 6b.

Slice 6.0.1 additions: `tepegoz-tui::app` +2 (`tab_on_pty_focus_forwards_tab_byte_to_pty_not_cycle` + `shift_tab_on_pty_focus_forwards_csi_z_to_pty_not_cycle`). Existing Tab/Shift-Tab cycle tests updated to preamble from Docker focus (via `Ctrl-b j` alias) so the cycle assertions don't collide with the new PTY carve-out.

Slice 6.0 additions: `tepegoz-tui::mouse` +12 (SGR parser: click + hover emission, 1-indexed coord conversion, wheel/right-click rejection, cross-chunk state, pass-through of non-SGR CSI); `tepegoz-tui::help` +3 (overlay renders five-binding surface + Slice-6.0.1 carve-out mention + dismissal hint + tiny-terminal fallback); `tepegoz-tui::scope::tests` +4 (border_style tri-state picker: focused / hovered / idle / focus-wins-over-hover); `tepegoz-tui::pty_tile` +5 (`[×]` close-affordance render + hit_test_tab_strip mapping for tab/close/empty/narrow strip); `tepegoz-tui::tile` +3 (`cycle_focus` forward/backward/too-small); `tepegoz-tui::input` +9 (bare Tab → FocusNext, bare Shift-Tab → FocusPrev, bare Esc pass-through, BareCsi collect for arrows/PageDown, removed-binding assertions for Ctrl-b 1..9/n/p/q/w/Enter); `tepegoz-tui::app` +13 (mouse-click dispatch on tile border / PTY tab / close affordance / scope row + double-click window + Tab tile-cycling + Tab cancels Docker confirm + plain Enter on Fleet replaces Ctrl-b Enter + help overlay toggle/Esc/click-dismiss/Ctrl-b-d-escape-hatch + SGR-bytes-through-parser round-trip). No new Linux-only tests in Slice 6.0 or 6.0.1.

Phase 5 additions: `tepegoz-tui` lib +28 (pane_stack state-machine, tab strip render, Ctrl-b keybind parsing, Fleet discovery-error hint, FIFO guard); `tepegoz-ssh` lib +14 (config + known_hosts); `tepegoz-proto::codec` +6 (Fleet + FleetAction + PaneTarget roundtrips); `tests/fleet_scope.rs` +3 (one always-on initial-state pin + one opt-in supervisor test with FleetAction extensions); `tests/remote_pane.rs` +1 opt-in; `tests/pane_stack.rs` +1 always-on (two-pane byte-isolation); `xtask` demo runner (no tests; engineering-ergonomics tool per CTO directive). No new Linux-only tests in Phase 5.

Phase 4 4d additions: `crates/tepegoz-core/tests/ports_processes_e2e.rs` +2 (combined Ports+Processes round-trip + docker-bound-port container-correlation, both opt-in). Post-4d desync fix (`77aa9ca`): `tepegoz-tui::session::stdin_pressure_does_not_desync_large_envelopes` default-suite regression pin (25 large ProcessList envelopes through a cancellation-stressed `select!` against a `UnixStream::pair`) + `crates/tepegoz-core/tests/desync_repro.rs` opt-in harness gated on `TEPEGOZ_DESYNC_REPRO=1` for future exploratory timing scenarios.

Phase 4 4c additions: `tepegoz-tui::app` +13 state-machine, `tepegoz-tui::scope::ports` +14 render. Cross-OS, no Linux-only additions. macOS 4c additions = 27; Linux 4c additions = 27.

Phase 4 4b additions: `tepegoz-proto::codec` +3, `tepegoz-probe::processes` +3, `tests/processes_scope.rs` +2. No new Linux-only tests in 4b (sysinfo is cross-OS). macOS 4b additions = 8; Linux 4b additions = 8.

- `tepegoz-proto::codec` — 17 (14 pre-4b + 3 new: `subscribe_processes_roundtrip`, `process_list_event_roundtrip_preserves_first_sample_cpu_none`, `processes_unavailable_event_roundtrip`)
- `tepegoz-probe` — 5 cross-OS (2 ports + 3 processes: `first_sample_returns_cpu_none_for_every_row`, `second_sample_returns_cpu_some_for_every_row`, `sample_contains_current_test_process`) plus 9 Linux-only cgroup-parser cases unchanged from 4a
- `tepegoz-pty` — 4 (unchanged)
- `tepegoz-docker` — 7 (unchanged)
- `tepegoz-tui::tile` — 13 (unchanged from C1.5b)
- `tepegoz-tui::input` — 22 (unchanged from C1.5b)
- `tepegoz-tui::scope::ports` — 14 (new in 4c: connecting/available/unavailable render per view, selection marker, CPU% em-dash vs number, UDP footer hint, empty-list messages, per-view help bar with toggle hint, filter bar caret, partial-row `?` cue)
- `tepegoz-tui::app` — 82 (69 pre-4c + 13 new in 4c: PortList/PortsUnavailable/ProcessList/ProcessesUnavailable event routing; `p` toggles views when focused + absorbs during filter input; per-view selection + filter independence; selection persistence by `(protocol, port, pid)` under reorder and entity-disappearance; selection persistence by `(pid, start_time)` under pid reuse; filter typing / backspace / enter / esc on Ports view; Ports-focused stdin routes to handle_ports_key not SendInput)
- `tepegoz-tui::pty_tile` — 3 (unchanged)
- `tepegoz-tui::scope::docker` — 14 (C3a's 11 + 3 new C3b cases: logs view renders status bar + transcript + help bar with logs keybinds; logs view renders the stream-ended marker + status `stream: ended: <reason>` + `tail: off`; logs view suppresses the confirm modal even when `pending_confirm` is Some)
- `tepegoz-tui::scope::placeholder` — 3 (unchanged)
- `tepegoz-tui::toast` — 5 (unchanged from C3a)
- `crates/tepegoz-core/tests/daemon_persistence.rs` — 2 (unchanged)
- `crates/tepegoz-core/tests/pty_persistence.rs` — 1 (unchanged)
- `crates/tepegoz-core/tests/pane_unsubscribe.rs` — 1 (unchanged)
- `crates/tepegoz-core/tests/vim_preservation.rs` — 1 (unchanged from C1.5b — vt100 reconstruction proxy; C1.5c passed the real-terminal eyeball check)
- `crates/tepegoz-core/tests/docker_scope.rs` — 8 (one new in C3c):
  - Subscribe(Docker) emits ContainerList xor DockerUnavailable (always-on)
  - Opt-in `TEPEGOZ_DOCKER_TEST=1` insists on Available for Subscribe(Docker)
  - DockerAction against unreachable engine returns Failure with non-empty reason (always-on)
  - Subscribe(DockerLogs) against unreachable engine emits DockerStreamEnded (always-on)
  - Subscribe(DockerStats) against unreachable engine emits DockerStreamEnded (always-on)
  - Opt-in `TEPEGOZ_DOCKER_TEST=1` end-to-end: provisions an alpine container, observes a log marker, observes a stats sample with mem_bytes > 0, performs Restart and asserts Success
  - **C2c3 latency pin** (opt-in): provisions a unique alpine container, subscribes to Docker, asserts first ContainerList arrives in <2 s *and* contains the provisioned container by name
  - **C3c Restart round-trip** (opt-in): provisions a unique alpine container, subscribes to Docker, snapshots pre-restart `state`/`status`, sleeps 2 s to let the "Up N seconds" counter advance, sends `DockerAction::Restart`, asserts matching `DockerActionResult::Success`, then asserts a subsequent `ContainerList` shows a shifted `state`/`status` for the container — pins the daemon's post-action refresh invariant. Verified locally against Docker Desktop; CI runs with `TEPEGOZ_DOCKER_TEST=1` unset so the test skips cleanly.
- `crates/tepegoz-core/tests/ports_scope.rs` — 2 (new in 4a):
  - `ports_subscription_emits_either_port_list_or_unavailable` (always-on): subscribes, asserts the daemon emits exactly one of `PortList { ports, source }` or `PortsUnavailable { reason }` within 30 s, and that the reason/source strings are non-empty (mirrors the Docker acceptance shape).
  - `ports_subscription_sees_locally_bound_listener_within_budget` (opt-in `TEPEGOZ_PROBE_TEST=1`): binds an ephemeral TCP listener inside the test process, subscribes to Ports, drains events until a `PortList` includes the bound `local_port`, then asserts the row attributes `pid == std::process::id()`, `protocol == "tcp"`, and non-empty `process_name`. 6 s budget covers one refresh boundary.
- `crates/tepegoz-core/tests/processes_scope.rs` — 2 (new in 4b):
  - `processes_subscription_emits_either_process_list_or_unavailable` (always-on): subscribes, asserts the daemon emits `ProcessList` xor `ProcessesUnavailable` within 30 s with non-empty source / reason, AND that every row of the first `ProcessList` carries `cpu_percent: None` (first-sample em-dash contract pinned at the integration level, not just the unit test).
  - `processes_subscription_sees_spawned_child_within_budget` (opt-in `TEPEGOZ_PROBE_TEST=1`): spawns a known `sleep 30` child (force-killed on Drop via `ChildGuard`), subscribes, drains events until a `ProcessList` includes the child's pid, asserts command contains "sleep", `start_time_unix_secs > 0`, and `mem_bytes > 0` (or `partial: true`). 5 s budget covers one refresh boundary.
- `crates/tepegoz-core/tests/ports_processes_e2e.rs` — 2 (new in 4d):
  - `ports_and_processes_see_spawned_child_and_see_it_disappear` (opt-in `TEPEGOZ_PROBE_TEST=1`): spawns a python3 child binding a loopback TCP port and sleeping, reads the assigned port from the child's stdout (handshake to avoid racing the probe against the bind), subscribes to BOTH Ports + Processes, asserts the child pid appears in both within 6 s, kills the child, asserts it disappears from both within 6 s. Uses `tokio::process::Command::kill_on_drop(true)` as the ChildGuard equivalent.
  - `docker_bound_port_surfaces_with_container_correlation` (opt-in on BOTH `TEPEGOZ_PROBE_TEST=1` AND `TEPEGOZ_DOCKER_TEST=1`): starts an alpine container publishing `<host_port>:80`, subscribes to Ports, asserts the row for `host_port` carries `container: Some(<id>)` within 6 s. Pins the README mockup's `:3000 web (docker)` distinguishing feature. Force-removes the container on Drop.
- `crates/tepegoz-core/tests/desync_repro.rs` — 1 (new with post-4d desync fix `77aa9ca`):
  - `desync_repro_with_select_tick_interrupting_read_envelope` (opt-in `TEPEGOZ_DESYNC_REPRO=1`): exploratory harness mimicking the pre-fix TUI main-loop shape (`read_envelope` called directly inside `tokio::select!` alongside tick / stdin branches). Not a substitute for the default-suite regression pin — the authoritative pin is `tepegoz-tui::session::tests::stdin_pressure_does_not_desync_large_envelopes`.

## Next phase

**Slice 6.0 (TUI input-surface simplification + mouse support) is the next slice** before Phase 6 starts. Product pivot per 2026-04-15 user sign-off: the multi-pane stack + tab strip + every Phase-5 feature that shipped stays; what changes is HOW users reach those features. Keyboard surface reduces to five documented bindings: `Tab` / `Shift-Tab` for tile focus (fixed cycle order: PTY → Docker → Ports → Fleet → ClaudeCode), arrows / j / k for row nav within focused tile, `Enter` primary action, `Esc` cancel, `Ctrl-b d` detach. `Ctrl-b h/j/k/l` + `Ctrl-b &` stay as undocumented aliases. `Ctrl-b 1..9` / `Ctrl-b n` / `Ctrl-b p` / `Ctrl-b w` / `Ctrl-b q` get removed — clickable tab strip + unified `Enter`/`Esc` obsolete them. Mouse bus: `AppEvent::MouseClick { x, y }` + `AppEvent::MouseHover { x, y }` on the existing bus; each tile gains a `hit_test(x, y, rect) -> Option<AppAction>`; crossterm mouse capture enabled in `terminal::enter_raw`. Hover states (border highlight, OSC 22 pointer cursor on supporting terminals) indicate clickability. Help overlay (`Ctrl-b ?`, long reserved via Phase 4 polish candidate) lands in 6.0 as the natural home for documenting the five-binding default + per-tile intra-keybinds. Decision #7 amended in-place (already uncommitted in `docs/DECISIONS.md` — picked up with the 6.0 commit per the cross-session edit-flow pattern); no new locked decisions.

**Phase 6 (agent binary + remote scope panels) follows Slice 6.0.** Phase 6's Slice 6a ships a `cargo xtask demo-phase-6 up/down` scaffold alongside the first meaningful implementation — the xtask-runner rule applies retroactively to Phase 5 and forward. Phases 5 and 6 are jointly the "make remote feel local" arc; Phase 6 swaps `RemotePane`'s per-pane russh-direct session for an agent-multiplexed single-session-per-host pattern without changing the v9 wire shape (the `OpenPane { target: Remote { alias } }` request shape continues to mean "open a remote pty against this host"; only the daemon-side backend swaps).

**Slice D (`DockerExec` → new pty pane) reopens as a v1.1 candidate** now that Phase 5 has concretely implemented the "scope → new pane" mechanism (pane-stack + tab strip + `Ctrl-b Enter` on Fleet → `OpenPane Remote`). DockerExec gets the same treatment — `Ctrl-b e` on a focused Docker row dispatches `DockerExec → OpenPane Local { shell: Some("docker exec -it <id> sh") }` + the new pane pushes onto the stack. Not a Phase 6 dependency; can land in parallel or as v1.1 cleanup.

## Phase 5 polish candidates (post-close follow-ups)

Phase 5 is closed; these are non-urgent follow-ups recorded here so future context rehydrates without re-deriving them from commit history.

- **Inline `OpenPane { Remote }` blocking** (5d-i carryover). Same-client commands serialize behind `state.remote_pty.open().await` for 1-3 s on remote pane opens while SSH dials. Not user-observed in scenarios 1-4 of the manual demo; scenarios 5-8 that could surface it weren't walked. Proper fix is a spawn + `request_id` correlation pattern (same shape as `DockerAction`), which naturally lands in Phase 6 wire v10 when agent-backed panes bring per-request ids anyway. Defer unless a user reports laggy-local-input-while-remote-opens.
- **Scenarios 5-8 eyeball verification.** End-to-end UX rendering on real SSH failures (red toast text, `⚠` glyph timing, `tepegoz doctor --ssh-forget` recovery loop) is machine-tested per layer but not manually walked. Slice 6.0 touches the TUI rendering layer (mouse + hover + help overlay) and would almost certainly surface any regression in these paths — fold a quick eyeball check into 6.0 verification. Full diagnostic path in `docs/ISSUES.md#phase-5-close-caveats-manual-demo-coverage-gap` if a user reports a real failure.

## Phase 4 polish candidates (post-close follow-ups)

Phase 4 is closed; these are non-urgent follow-ups recorded here so future context rehydrates without re-deriving them from commit history.

- **Help overlay for focus-navigation keybinds.** `Ctrl-b ?` has been reserved on the event bus since C1.5b and currently renders as a debug-log no-op. Wire the overlay to list `h/j/k/l` focus-nav (**both horizontal AND vertical** — not just vertical, which was the discoverability miss during the Phase 4 demo) + `d`/`q` detach + each per-tile's visible keybinds (Docker: `r`/`s`/`K`/`X`/`l`/`/`; Ports: `p`/`/`; PTY: passthrough). Modal overlay inside the god-view grid, same shape as the Docker confirm modal. Priority: early v1.1 polish, or land as part of any Phase 5-adjacent TUI touch.

## Phase 3 polish candidates (post-close follow-ups)

Phase 3 is closed; these are non-urgent follow-ups recorded here so future context rehydrates without re-deriving them from commit history. All three remain valid; pick up when signal says so.

- **Bounded `tail_lines` default for `Subscribe(DockerLogs)`.** Current code sends `tail_lines: 0` on `l` press, which is "all history" per Slice B's wire contract. For a chatty long-lived container this dumps megabytes on entry. Default to `tail_lines: 1000` with a "load more" affordance (e.g. PgUp past the top triggers a fetch of older lines). No wire change needed — daemon already supports any value.
- **Logs tile zoom / full-screen toggle.** If the user's real-terminal eyeball check (Scenario 9) reveals the Docker tile's ~40×17 cell area is too cramped for usable log viewing (long lines truncate, JSON / stack traces become unreadable), add a keybind (candidate: `z`) that temporarily expands the Docker tile to the full scope row (still preserves the PTY tile above — doesn't violate Decision #7's "all scopes visible"). Explicit non-goal for v1 unless the eyeball check demands it; deferred to v2 otherwise.
- **Logs color palette for color-deficient terminals.** Currently `LogLine` renders with Color::Gray for stdout and Color::Yellow for stderr plus a ` ` / `!` prefix. The prefix provides a shape cue for color-deficient users, but if real-user feedback flags readability (pale-yellow stderr on dark themes, gray-on-gray on light themes), revisit the palette or make it theme-configurable. Defer until feedback.
