# Status

## Current state

**Phase 3 in progress — C3a (actions + confirm modal + toasts + timeout sweep) landed; C3b (logs panel) + C3c (end-to-end test + manual demo) outstanding.** Slices A + B + C1 + C2 + C1.5a + C1.5b + **C1.5c (user manual demo passed)** + **C3a** have landed on `main`. The TUI is a fixed tiled god view per Decision #7: PTY tile (top, full width) + Docker tile (bottom-left, now with action keybinds + inline confirm modal + toast overlay) + Ports placeholder (bottom-middle, Phase 4) + SSH Fleet placeholder (bottom-right, Phase 5) + Claude Code placeholder (bottom-full-width strip, Phase 9). All subscriptions live concurrently; focus moves between tiles via `Ctrl-b h/j/k/l` + arrow keys. The pty tile renders via `vt100::Parser` → ratatui cells; no more raw passthrough. Scope renderers take `(state, Frame, Rect, focused)` per the rendering contract in ARCHITECTURE §9.

C3a delivered: `r`/`s` immediate dispatch of `DockerAction::Restart` / `Stop` on the focused Docker tile; `K`/`X` enter an inline confirm modal (centered bordered box inside the Docker tile's `Rect`, never full-screen) — `y`/`Y` confirms + dispatches, any other key cancels; focus-away from the Docker tile cancels; 10 s idle auto-cancel; pending `DockerAction` entries tracked by `request_id` in `App::pending_actions` with a 30 s timeout sweep driven on every Tick; a `Success` result emits a green "`<verb> <name>` — succeeded" toast keyed off the pending action's description, a `Failure { reason }` emits a red "`<verb> <name>` failed: `<reason>`" toast verbatim from dockerd; stale results (no matching pending action) fall back to `<verb> <container_id>`; toast overlay renders as a 1-line-per-toast strip directly above the Claude Code tile, max 3 visible, 4th arrival drops the oldest silently, per-kind auto-dismiss (Success ~3 s, Error ~8 s, Info ~4 s), never blocks keystrokes; `Payload::Error` from the daemon also lands in the toast overlay queue; help bar swaps to the action-keybinds hint when idle, the confirm hint when confirming, the filter hint when filter-editing; `AppEvent::PendingActionTimeout(u64)` wire retained on the event surface for a future dedicated sweeper. Wire protocol unchanged at v4.

C1.5c passed on the user's real-terminal run: default god-view layout renders on first launch with no config; focus nav (`Ctrl-b h/j/k/l` + arrows) is natural; vim in the pty tile renders + survives focus movement + detach/reattach; Docker tile populates within ~2 s and stays live concurrent with pty; engine-unavailable-mid-session recovery works; tiny-terminal fallback renders cleanly. The C2c3 eyeball demo (vim-preservation + CTO §7 engine-unavailable) folded into C1.5c and is retired.

Phase 2 closed cleanly: the "immediate-detach" report turned out to be user confusion (attached pane shell is visually identical to the outer shell) — see `docs/ISSUES.md` resolved entry. TUI paints an OSC 0 window title (`tepegoz · pane N`) on attach so the pane is unmistakable.

## Phase matrix

| # | Name | Status | Commit(s) | Acceptance test(s) |
|---|---|---|---|---|
| 0 | Scaffold | ✅ | `81c7731` | `tepegoz --help`, green CI |
| 1 | Proto + daemon + TUI round-trip | ✅ | `3715bf9` | `daemon_persistence.rs` |
| 2 | Local pty multiplex + persistence | ✅ | `eab274c`, `321ed5e` | `pty_persistence.rs`, `subscribe_does_not_duplicate_bytes`, `pane_honors_cwd_and_exposes_pane_id_env` |
| 3 | Docker scope panel | 🟠 (A + B + C1 + C2 + C1.5a + C1.5b + C1.5c + C3a landed; C3b + C3c outstanding) | `24dc244`, `816765b`, `e4d2113`, `664703f`, `43b28eb`, `56a8a4f`, `c7c0d39`, `a5276cc`, `2c54c44`, `c7b336d`, _C3a HEAD_ | 143 tests (109 TUI lib + 7 docker crate + 11 proto codec + 4 pty + 2 daemon_persistence + 7 docker_scope + 1 pty_persistence + 1 pane_unsubscribe + 1 vim_preservation/vt100-reconstruction) |
| 4 | Ports + processes panels (local) | ⚪ | — | — |
| 5 | SSH transport + remote pty | ⚪ | — | — |
| 6 | Agent binary + remote scopes | ⚪ | — | — |
| 7 | Port scanner | ⚪ | — | — |
| 8 | Recording + replay | ⚪ | — | — |
| 9 | Claude Code pane awareness | ⚪ | — | — |
| 10 | QUIC hot path + release 0.1.0 | ⚪ | — | — |

Status key: ✅ complete · 🟡 code+tests green, user acceptance pending · 🟠 in progress · ⚪ not started.

## What works end-to-end

- Daemon binds user-scoped Unix socket at `$XDG_RUNTIME_DIR/tepegoz-$uid/daemon.sock` (fallback `$TMPDIR` or `/tmp`). Parent dir `0700` when default path (we own it), socket `0600`. Override paths leave parent perms alone.
- Stale-socket eviction on startup. Refuses to start under another live daemon. Graceful SIGINT shutdown with socket cleanup.
- Wire protocol v4: rkyv-archived `Envelope { version, payload }`, 4-byte big-endian length prefix, bytecheck validation on every read. Messages:
  - Commands: `Hello`, `Ping`, `Subscribe(Status | Docker | DockerLogs | DockerStats)`, `Unsubscribe`, `OpenPane`, `AttachPane`, `ClosePane`, `ListPanes`, `SendInput`, `ResizePane`, `DockerAction`
  - Responses: `Welcome`, `Pong`, `PaneOpened`, `PaneList`, `DockerActionResult`, `Error`
  - Events (in `Event(EventFrame)`): `Status`, `PaneSnapshot`, `PaneOutput`, `PaneExit`, `PaneLagged`, `ContainerList`, `DockerUnavailable`, `ContainerLog`, `ContainerStats`, `DockerStreamEnded`
- PTY manager owns panes; per-pane 2 MiB ring buffer (`VecDeque<Bytes>` with total-byte accounting, oldest-chunks-evict on overflow); per-pane reader + waiter threads; `tokio::sync::broadcast` channel for subscribers.
- Docker subscription: per-`Subscribe(Docker)` task walks platform socket candidates (`$DOCKER_HOST` env > Docker Desktop > Colima > Rancher Desktop > native Linux), pings the first reachable engine, and emits a `ContainerList` immediately plus every 2 s. On unreachable engine: emits `DockerUnavailable { reason }` once per availability transition, retries `Engine::connect` every 5 s. Survives `dockerd` restarts; survives the user starting docker after subscribing.
- Docker lifecycle actions: `Payload::DockerAction { request_id, container_id, kind }` runs in a spawned task so a slow daemon doesn't stall the session loop. Always replies with `DockerActionResult { request_id, container_id, kind, outcome }` — engine-unavailable and bollard errors both surface as `Failure { reason }` (verbatim from dockerd; daemon doesn't try to classify). `Remove` is force-remove (`docker rm -f` semantics).
- Docker logs streaming: per-`Subscribe(DockerLogs)` task opens a bollard log stream (stdout + stderr) and forwards each chunk as `Event::ContainerLog { stream, data }`. Always emits a terminal `Event::DockerStreamEnded { reason }` — engine unreachable, container exit, container removal — so a UI knows the stream is done and never spins waiting for chunks that won't come.
- Docker stats: per-`Subscribe(DockerStats)` task streams bollard stats (~1/sec). CPU% is computed from cpu/precpu deltas using the standard docker-stats-CLI formula; `0.0` whenever the calculation can't be performed (first sample, missing precpu on Windows, sys_delta=0). Like logs, terminates with `DockerStreamEnded`.
- Daemon client session: single dedicated writer task drains an mpsc of outgoing envelopes — no per-write locks. **Subscription uniformity**: pane and docker subscriptions share the same `HashMap<id, AbortHandle>` shape (`pane_subs` and `docker_subs`). `Unsubscribe { id }` cancels across both maps with a single check. Phase 4 (Ports/Processes), 5 (SSH remote pty), 7 (port scanner) inherit this shape — every new subscription kind adds a map alongside, not a new mechanism. (Through Slice C1 `pane_subs` was a `JoinSet<()>` with no per-id key, so `Unsubscribe` of a pane sub silently no-op'd; the C1 TUI's synthetic re-attach was leaking one zombie forwarder per Scope→Pane mode switch. Fixed in the C2-gate commit `43b28eb`; regression test `pane_unsubscribe.rs` pins the invariant.)
- TUI architecture: pure-state-machine `App` (in `tepegoz-tui/src/app.rs`) handles every external happening as an `AppEvent` (StdinChunk, DaemonEnvelope, Resize, Tick, PendingActionTimeout) and emits `AppAction`s (SendEnvelope, DrawFrame, FocusTile, Detach{User|PaneExited}, ShowToast{kind,message}). The `AppRuntime` (in `tepegoz-tui/src/session.rs`) executes those actions against the daemon socket, stdin/stdout, and ratatui's terminal. State-machine tests live in `app::tests`; headless render tests in `scope::docker::tests`, `scope::placeholder::tests`, `pty_tile::tests`, and `tile::tests`.
- TUI view model (Decision #7): fixed tiled god view, no mode switching. `View { layout: TileLayout, focused: TileId }`. Five tiles on a 120×40+ terminal: PTY (top full-width), Docker (bottom-left), Ports placeholder "Phase 4" (bottom-middle), SSH Fleet placeholder "Phase 5" (bottom-right), Claude Code placeholder "Phase 9" (bottom-full-width strip). Below 80×24 the layout collapses to a single "Terminal too small" fallback tile. All subscriptions (`AttachPane`, `Subscribe(Docker)`, later Ports/Fleet/ClaudeCode) live concurrently for the life of the TUI.
- TUI focus navigation: `Ctrl-b h/j/k/l` + `Ctrl-b` + arrow keys move focus between adjacent tiles (spatial adjacency with left-align tiebreak, so `Ctrl-b j` from full-width PTY lands on Docker rather than Ports). `Ctrl-b d` / `Ctrl-b q` detach from any tile. The focused tile owns the keystroke stream; unfocused tiles continue to update live. Input routing per tile kind: Pty → `SendInput`; Scope(Docker) → scope key parser (j/k/arrows/g/G/Home/End/`/`+filter); Placeholder/TooSmall → drop. Placeholder tiles show a "Phase N — not yet implemented" hint when focused.
- Pty tile rendering: pty bytes from `Event::PaneOutput` / `Event::PaneSnapshot` feed a `vt100::Parser` owned by the App. The pty tile renderer projects the parser's screen buffer (cells + fg/bg colors + bold/italic/underline/reverse attributes) into ratatui cells within the tile's `Rect`. Cursor rendered as a reversed cell only when focused (unfocused tiles show the buffer without a misleading caret). Parser is resized on every Resize event to match the pty tile's new dimensions; corresponding `ResizePane` envelope sizes the daemon-side pane to match the tile (NOT the full terminal), so vim et al. render inside the box.
- Scope rendering contract (ARCHITECTURE §9): every scope renderer takes `(state, Frame, Rect, focused: bool)` and draws only within its `Rect`. Focused tile borders are bright cyan; unfocused are dim gray. `scope::docker::render` preserves all C2c2 rendering logic (three-state lifecycle: Connecting/Available/Unavailable, container table with `▶` selection marker, filter bar with `_` caret when active, help bar). `scope::placeholder::render` draws a bordered block with centered label + focused hint. Phases 4/5/7/9 inherit — their tile slots into the god view at its placeholder and is replaced by the live renderer when the phase lands.
- TUI action keybinds (C3a): focused Docker tile recognizes `r`/`R` (Restart) and `s`/`S` (Stop) for immediate dispatch, plus `K` (Kill) and `X` (Remove) which first enter an inline confirm modal per CTO §4. Dispatch allocates a request id via the shared monotonic allocator, inserts a `PendingAction { deadline: now + 30 s, description }` keyed by that id, and sends `Payload::DockerAction(DockerActionRequest { request_id, container_id, kind })`. Keybinds no-op when Docker is Unavailable or the visible list is empty; focus on a non-Docker tile routes the keys to the normal input path (PTY's `SendInput`, placeholder drop).
- TUI confirm modal (C3a, UX clarification #3): centered bordered box rendered *inside* the Docker tile's `Rect` — never full-screen, other tiles keep rendering and receiving live updates throughout. Prompt text: "`<Kill|Remove>` container `<name>`? [y] confirm · any other key cancels". `y`/`Y` clears the confirm and dispatches the `DockerAction`; any other key clears the confirm without dispatching; focus moving away from the Docker tile cancels; a 10 s idle deadline clears the confirm on the next Tick sweep. Help bar swaps to the confirm-specific hint while active.
- TUI toast overlay (C3a, UX clarification #2): rendered as a 1-line-per-toast strip positioned directly above the Claude Code tile (full scope-row width). `App::toasts: VecDeque<Toast>` is bounded to 3 — a 4th arrival `pop_front`s the oldest silently so the user's keystrokes are never gated on a toast. Per-kind auto-dismiss: Success ~3 s, Error ~8 s, Info ~4 s. Toasts are `Success`/`Error`-colored content with a short `ok:` / `err:` / `info:` prefix. The 30 Hz Tick drives the sweep that retires expired entries; state transitions that want to surface a toast call `App::push_toast(kind, message, &mut actions)` which bounds the queue, appends `AppAction::ShowToast { kind, message }` for the runtime's `tui.log` trace, and emits `DrawFrame`.
- TUI pending-action lifecycle (C3a): on every 30 Hz Tick, `App::sweep_expired(Instant::now(), …)` walks `pending_actions` and drops entries whose `deadline` has passed, each expiring entry emits an "`<verb> <name>` timed out — check engine" Error toast. `DockerActionResult { request_id, kind, outcome }` looks up (and removes) the entry by `request_id`: `Success` → green "`<verb> <name>` — succeeded" toast; `Failure { reason }` → red "`<verb> <name>` failed: `<reason>`" toast (reason verbatim from dockerd); stale results (no matching pending action) fall back to `<verb> <container_id>` so the user still sees the outcome. `Payload::Error` from the daemon lands in the same overlay queue. `AppEvent::PendingActionTimeout(u64)` is retained on the event surface for a future dedicated timer sweeper without reshaping the API.
- TUI redraw cadence: 30 Hz `tokio::time::interval` always-on (no mode gating — the tile grid is always live). App responds to every `Tick` with `AppAction::DrawFrame`; the runtime draws the entire tile grid via ratatui's buffer diff (redundant-cell writes are cheap). State changes (pty output, container list update, focus move, filter edit) also emit `DrawFrame` directly so updates don't wait for the next tick.
- Daemon handshake: rejects protocol-version mismatches at the socket level. Both `Envelope.version` and `Hello.client_version` must equal the daemon's `PROTOCOL_VERSION`; otherwise the daemon sends a structured `Error(VersionMismatch)` naming both versions and closes. Without this guard a v3 client connecting to a v4 daemon would silently handshake and later trip an opaque rkyv decode error.
- Panes inherit `TEPEGOZ_PANE_ID=<id>` in env. TUI refuses to run if its own env has that var (prevents recursive attach feedback loop).
- Shells spawn in the TUI's `current_dir()` rather than `$HOME` (portable-pty's default).
- TUI sets the terminal window title to `tepegoz · pane N` on attach (OSC 0) and clears it on detach, so an attached pane is visually distinct from the outer shell.

## Test coverage (143 tests, all green)

- `tepegoz-proto::codec` — 11 (unchanged)
- `tepegoz-pty` — 4 (unchanged)
- `tepegoz-docker` — 7 (unchanged)
- `tepegoz-tui::tile` — 13 (unchanged from C1.5b)
- `tepegoz-tui::input` — 22 (unchanged from C1.5b)
- `tepegoz-tui::app` — 51 (C1.5b's 29 + 22 new C3a cases: `r`/`s` dispatch immediately with correct kind + container_id + pending-action entry; `K`/`X` enter pending_confirm without dispatching; `y`/`Y` during confirm dispatches + clears; `n` / `Esc` / random-char during confirm cancel; focus-away from Docker cancels confirm; 10 s confirm deadline drops on sweep; `r` with PTY focused routes to SendInput, not DockerAction; `r` no-op when Docker Unavailable or list empty; DockerActionResult::Success toasts with description; DockerActionResult::Failure toasts with description + verbatim reason; DockerActionResult without matching pending action falls back to container id; 30 s action timeout emits Error toast + drops entry; `PendingActionTimeout(id)` event expires that action; `PendingActionTimeout` for unknown id is a silent no-op; 4th toast drops oldest silently; toast sweep drops expired toasts per-kind; daemon Error payload lands in the overlay queue)
- `tepegoz-tui::pty_tile` — 3 (unchanged)
- `tepegoz-tui::scope::docker` — 11 (C1.5b's 8 + 3 new C3a cases: pending_confirm renders the confirm modal with container name + `[y] confirm` hint; confirm modal is absent without pending_confirm; help bar shows `[r] restart · [s] stop · [K] kill · [X] remove` in the idle state)
- `tepegoz-tui::scope::placeholder` — 3 (unchanged)
- `tepegoz-tui::toast` — 5 new in C3a: empty toast list paints no cells; single Error toast renders on the row directly above the Claude Code strip; three toasts stack on three lines; a `MAX_TOASTS + 2`-entry list caps visible at `MAX_TOASTS` (newest kept, oldest suppressed); too-small fallback layout (no ClaudeCode tile) no-ops rather than panicking
- `crates/tepegoz-core/tests/daemon_persistence.rs` — 2 (unchanged)
- `crates/tepegoz-core/tests/pty_persistence.rs` — 1 (unchanged)
- `crates/tepegoz-core/tests/pane_unsubscribe.rs` — 1 (unchanged)
- `crates/tepegoz-core/tests/vim_preservation.rs` — 1 (unchanged from C1.5b — vt100 reconstruction proxy; C1.5c passed the real-terminal eyeball check)
- `crates/tepegoz-core/tests/docker_scope.rs` — 7 (unchanged):
  - Subscribe(Docker) emits ContainerList xor DockerUnavailable (always-on)
  - Opt-in `TEPEGOZ_DOCKER_TEST=1` insists on Available for Subscribe(Docker)
  - DockerAction against unreachable engine returns Failure with non-empty reason (always-on)
  - Subscribe(DockerLogs) against unreachable engine emits DockerStreamEnded (always-on)
  - Subscribe(DockerStats) against unreachable engine emits DockerStreamEnded (always-on)
  - Opt-in `TEPEGOZ_DOCKER_TEST=1` end-to-end: provisions an alpine container, observes a log marker, observes a stats sample with mem_bytes > 0, performs Restart and asserts Success
  - **C2c3 latency pin** (opt-in): provisions a unique alpine container, subscribes to Docker, asserts first ContainerList arrives in <2 s *and* contains the provisioned container by name

## Next (slice)

**Slice C3b — logs panel as Docker-tile sub-state.** `l` on the focused Docker tile switches the tile's content from the container table to a scrolling log transcript for the currently selected container. Entry: `Subscribe(DockerLogs { id, container_id, follow: true, tail_lines: 0 })` + transition the Docker tile to `LogsView { container_id, sub_id, buffer, scroll_offset, at_tail }`. Exit (`Esc` or `q`): `Unsubscribe { id }` + transition back to the list view. `ContainerLog { stream, data }` appends into the buffer; `DockerStreamEnded { reason }` renders a terminal "— log stream ended: `<reason>` —" line and disables auto-tail. Navigation: `j`/`k`/`PgUp`/`PgDn` scroll; `G` jumps to bottom + re-enables tail. Other tiles (PTY, placeholders, the scope row) keep rendering and receiving input throughout — preserves Decision #7's all-scopes-visible invariant. Modal-free; stays within the tile's `Rect`.

**Slice C3c — end-to-end integration test + manual demo.** New `crates/tepegoz-core/tests/` test (opt-in `TEPEGOZ_DOCKER_TEST=1`) that provisions a unique alpine container, drives `DockerActionKind::Restart` end-to-end (daemon socket + DockerAction → DockerActionResult::Success), asserts a follow-up `ContainerList` reflects the change (state/status shift). `docs/OPERATIONS.md` gets a C3 manual-demo script for the user's eyeball check: `r`/`s` on the focused Docker tile, `K`/`X` + confirm flow, failure toast verbatim text, timeout toast on engine stall, toast stacking + drop-oldest, logs panel entry/exit/tail. After C3c is CI-green on both OSes and pushed, the user runs the manual demo; once signed off, Phase 3 goes ✅.

**Slice D deferred pending design pass.** Under the tiled layout, `DockerExec` → "open a new pane" is architecturally non-trivial (the fixed layout has exactly one pty tile). Decision on how exec panes fit into the god view needs to land before any Slice D code.
