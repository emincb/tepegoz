# Handoff

Session-boundary handshake between the CTO (planner/architect) and the engineer. Both sides write here before their context clears; both sides read both sections when starting a fresh session.

Not a journal. Not a changelog. Captures *in-flight thinking that isn't in the canonical docs yet*:

- `docs/STATUS.md` is authoritative for current phase state.
- `docs/DECISIONS.md` is authoritative for locked architectural commitments.
- `docs/ROADMAP.md` is authoritative for phase plans.
- `docs/ISSUES.md` is authoritative for active bugs.
- **HANDOFF.md is authoritative for what's in the CTO's or engineer's head that hasn't yet crystallized into those docs.**

When docs and HANDOFF conflict, docs win. Update HANDOFF (or delete the stale entry), don't act on stale planning notes.

---

## CTO section

**Last updated:** 2026-04-15, post-5d-i sign-off. 5d-i at `e2f1b06`: wire v9 `PaneTarget::{Local, Remote { alias }}` on `OpenPaneSpec`, daemon `RemotePaneManager` + `RemotePane` with single-task-owned russh channel driver, parallel managers (local 1+ / remote 1<<32+ PaneId namespaces, order-dependent dispatch), per-pane fresh SSH session (Phase 6 consolidates), ISSUES.md "Known limitations, Phase 6 upgrade path" section. 252 tests on macOS, CI green all 7 jobs. 5d-ii (TUI pane-stack + tab strip + keybinds + `tepegoz connect` CLI) kicked off; **engineer memory was cleared after 5d-i push, so the 5d-ii relay is self-contained with full scope restated**. **Fresh CTO is reading cold — 5d-ii relay already sent; engineer is implementing.**

### What I just signed off on
- **Slice 5d-i landed** (`e2f1b06`). Wire v9 with `OpenPaneSpec.target: PaneTarget` field (`#[derive(Default)]` + `#[default] Local`, clippy-driven `derivable-impls` pattern). `RemotePaneManager` + `RemotePane` in new `tepegoz-core::remote_pane` module mirror `tepegoz_pty::PtyManager` / `Pane` exactly for subscribe/send_input/resize/close surface. Bytes flow through the same `broadcast::Sender<PaneUpdate>` shape; `forward_remote_pane` structurally parallels `forward_pane` (60 LOC dup acknowledged, shared `PaneBackend` trait extraction deferred to Phase 6 when agent-backed becomes a third implementor). Parallel managers over trait extraction — `SharedState` gains `remote_pty: RemotePaneManager` alongside existing `pty: PtyManager`; `handle_command` checks `remote_pty` first, falls through to `pty`. Disjoint PaneId namespaces (local 1+, remote 1<<32+) for visual separation in logs; real uniqueness comes from allocation-per-manager + order-dependent dispatch. Single-task-owned russh channel via `drive_channel` — `tokio::select!` (biased) over `cmd_rx.recv()` + `channel.wait()` — cancellation-safe by construction (russh's `channel.wait()` is backed by a tokio mpsc receiver). ChannelMsg::ExtendedData merges with stdout (correct pty semantics). Per-pane fresh SSH session via `connect_host` + `request_pty + request_shell`, `_session: Arc<SshSession>` keeps russh Handle alive for the channel's lifetime. All 5a TOFU / auth / ProxyJump-pre-check invariants apply. ISSUES.md new "Known limitations, Phase 6 upgrade path" section documents the two Phase 5 scope boundaries (remote-pane dies on SSH disconnect; per-pane SSH connection overhead) with Phase 6 agent mechanism named for each + ctrl-b-w overlay deferred to 5e/v1.1. Opt-in integration test `remote_pane_open_attach_exec_roundtrip` drives full pipeline against openssh-server. 252 tests on macOS. CI green all 7 jobs.
- **One non-blocking concern flagged**: `handle_command`'s `OpenPane { Remote }` arm awaits `state.remote_pty.open()` INLINE — 1-3s of blocking on connect + auth + pty-req. During that window, same-client commands serialize behind the remote open. Different clients unaffected. Engineer's comment correctly identifies the alternative (spawn + PaneOpening correlation) as complicating ordering with subsequent AttachPane. Accept for 5d-i; flag for 5d-ii user-test: if the 1-3s block feels laggy when user has active local pane + opens remote pane, revisit with spawn + request_id correlation pattern (same shape as DockerAction). Don't pre-optimize.
- **Slice 5c-ii landed** (`7120c44`). Wire v8 bump: `Payload::FleetAction(FleetActionRequest)` + `Payload::FleetActionResult(FleetActionResult)` mirror DockerAction shape for pending-action correlation + toast UX. `FleetActionKind::{Reconnect, Disconnect}` + `FleetActionOutcome::{Success, Failure { reason }}`. `Event::HostStateChanged` gains `reason: Option<String>` (Option A per CTO recommendation) — populated only for terminal states, `debug_assert!` in `emit_state` pins the invariant. `HostState::is_terminal()` helper on the wire type used by both daemon assertions and TUI toast gating. Supervisor rewritten to `tokio::select!`-driven phases (`ConnectedOutcome`, `BackoffOutcome`, `TerminalReset` enums per phase) with per-host `mpsc::UnboundedSender<HostAction>` kept in `host_senders` by the coordinator. **Connect future cancellation safety via spawn + oneshot** — engineer applied the Phase 4 desync lesson preemptively since russh's `client::connect` cancellation-safety isn't documented. Coordinator broadcasts FleetAction to all active Fleet subs (clients typically hold one) with clean unknown-alias Failure path pointing at `tepegoz doctor --ssh-hosts`. TUI `Ctrl-b r` dispatches FleetAction::Reconnect via `dispatch_fleet_reconnect` mirroring `dispatch_docker_action`; capital `R` is no-op per capital-discipline (reconnect is safe). FleetActionResult Success emits `Info`-kind toast ("dispatched") because actual connection outcome arrives via HostStateChanged seconds later with its own state-change toast — nice semantic precision. ProxyJump re-emits AuthFailed-with-reason on each Reconnect attempt so user gets fresh toast explaining why. Toast gating: `state.is_terminal() && reason.is_some() && prev_state != Some(state)` — transitions INTO terminal only. Docs folded: ARCHITECTURE §5 Paths table gets known_hosts row + `TEPEGOZ_CONFIG_DIR`/`TEPEGOZ_DATA_DIR` in Overrides + cleaned up stale `(planned)` on Config row; OPERATIONS.md adds autoconnect example + env-overrides subsection. Integration test extension: FleetAction::Reconnect succeeds against known alias + unknown-alias path returns Failure with legible reason. 250 tests on macOS. CI green all 7 jobs.
- **One 5e polish item flagged**: toast-gating comment claims "re-toast if reason changed" but the gate is state-only. Engineer picks between Option A (implement reason-diff retoasting, track prev_reason alongside prev_state) or Option B (remove the exception claim from the comment; accept state-only gating as v1 behavior — my preference). Non-blocking, 5e cleanup.
- **Slice 5c-i landed** (`fd0e50e`). Per-host supervisor architecture in `tokio::task::JoinSet` (subscription-cancellation drops the set, aborts every supervisor — no per-task AbortHandle bookkeeping). State machine matches spec exactly: Disconnected → Connecting → Connected → (Degraded) → Disconnected → backoff → retry. Named constants for every timing knob (`SSH_HEARTBEAT_INTERVAL = 30s`, `SSH_BACKOFF_LADDER = [1,2,5,15,60]s`, `SSH_RECONNECT_RESET_THRESHOLD = 30s`, miss thresholds 1→Degraded / 3→Disconnected). Heartbeat double-signal (`handle.is_closed()` + `send_keepalive(true).await.is_ok()`) with `MissedTickBehavior::Skip` + consume-immediate-tick pattern + Degraded→Connected bounce-back on single-miss-recovery. russh 0.60's fire-and-forget keepalive documented as the ~90s worst-case detection bound. ProxyJump pre-check transitions straight to `HostState::AuthFailed` terminal (5a follow-up #1 landed). `autoconnect: HashSet<String>` on `HostList` daemon-side only, populated from tepegoz config.toml source. Env-var overrides `TEPEGOZ_CONFIG_DIR` + `TEPEGOZ_DATA_DIR` short-circuit `dirs::` lookups (motivated by macOS's `dirs::config_dir()` ignoring `XDG_CONFIG_HOME`).
- **Slice 5b closed end-to-end** across three commits:
  1. **`f038cee`** (feature) — wire protocol v7 with `Subscription::Fleet` + `Event::HostList` + `Event::HostStateChanged`; `HostEntry` migrated to `tepegoz-proto` (single-type with re-export, `identity_files: Vec<String>` for rkyv round-trip); `HostState` 8-variant enum (4 user-visible + 2 transient + 2 Phase 6 additive). Daemon `fleet_subs` + `forward_fleet` (spawn_blocking discovery, HostList + Disconnected-per-host, park until cancelled — 5c replaces the park). Fleet tile renderer with Q6 four-glyph state set (● ◐ ○ ⚠) + selection + filter + em-dash procs placeholder + first-run hint. `tepegoz doctor --ssh-hosts` CLI. All five 5a follow-ups folded correctly (tilde expansion, 0600 mode, cert skip message, Include directive mitigation via `strip_include_directives` + self-documenting pin test, ProxyJump deferred to 5c with doctor annotation). OPERATIONS.md gains "SSH Fleet discovery (Phase 5 Slice 5b)" section with Q2 precedence + examples + diagnostic + limitations + host-key TOFU. 241 tests on macOS (+15 from 5a's 226).
  2. **`54d1c29`** (ring-backend cross-build fix) — `aws-lc-sys` (default russh backend) fails `cargo-zigbuild` Linux→`x86_64-apple-darwin` at the linker; 5a didn't trigger it because tepegoz-ssh wasn't linked into consumer binaries, 5b plumbs it into the daemon + main. Switch `russh` to `default-features = false` + `features = ["flate2", "ring", "rsa"]`. Ring is the rustls/quinn/reqwest ecosystem default, mostly-pure-Rust, covers all modern-OpenSSH-negotiated algorithms (ed25519, ecdsa-p256/384/521, RSA-SHA-256+). Auth chain behavior identical to 5a. Rationale preserved in Cargo.toml comment. Reference-model diagnose-before-fix execution: engineer identified the specific CI step, traced the link-boundary regression to 5b's consumer plumbing, chose ring with explicit feature list + documentation — no symptom-papering. Forward note: ring doesn't support legacy algorithms (DSA, RSA-SHA-1, 3DES); modern OpenSSH doesn't either; reopen path is feature-flagged aws-lc-rs in v1.1 if a user surfaces legacy-hardware SSH.
  3. **`f255681`** (--ssh-forget addition, CTO Option A). `KnownHostsStore::forget(hostname, port) -> Result<usize, SshError>`. Only removes single-host entries in the exact shape `trust` writes (`host` for port 22, `[host]:port` otherwise). Multi-host comma-separated patterns, hashed `|1|...` entries, user hand-edits — all preserved because their first-token form doesn't match a literal hostname (correct by construction, not by special-casing). Idempotent on missing file (`Ok(0)`). Re-applies 0600 via extracted `chmod_0600` helper. CLI `tepegoz doctor --ssh-forget <ALIAS>` resolves through `HostList::discover()`, opens tepegoz known_hosts, prints count + path + re-TOFU reminder — honest, auditable. 5 lib tests cover happy path + idempotent + port-bracket encoding + multi-host preservation + 0600 re-apply. 246 tests total on macOS. Docs+error+CLI now consistent.
- **Three 5e polish items carried forward** in both the `f255681` commit message and below, so they survive context clears.
- **Slice 5a landed** (`2b54d2e`). `tepegoz-ssh` crate: `config` (Q2 discovery precedence, russh-config per-host resolution, wildcard + negation skipped, case-insensitive `Host` keyword), `known_hosts` (tepegoz-owned TOFU, OpenSSH-compatible format, never touches `~/.ssh/known_hosts`), `session` (russh 0.60 client, `TofuHandler` with `Arc<Mutex<TofuOutcome>>` verdict side-channel, agent → IdentityFile auth chain, `KeyIsEncrypted` → verbatim attempts-list reason per Q4), `error` (`HostKeyMismatch` carries path + line, `AuthFailed.reason` accumulates full method chain for 5c's red toast), `paths` (OS-split via `dirs`). 14 cross-OS lib tests + 1 opt-in integration (`TEPEGOZ_SSH_TEST=1`) using bare docker CLI + `ContainerGuard`. `ARCHITECTURE.md §8` gained "SSH auth (Phase 5)" + "SSH host-key TOFU (Phase 5)" bullets with explicit separation from Decision #2 AND Phase 6 agent TOFU.
- **Phase 4 closed** (`357a016`) + **diagnostic tracing cleanup** (`1069e95`). User ran the 8-scenario manual demo on the desync-fix binary and signed off. STATUS row 4 ✅, ROADMAP Phase 4 marker ✅ (2026-04-14), 211 tests on macOS / 220 on ubuntu.
- **Phase 5 proposal (7 questions) reviewed and signed off.** Engineer's answers strong on Q1/Q3/Q4/Q7. Adjustments folded: Q2 adds macOS config path + `tepegoz doctor --ssh-hosts` diagnostic + OPERATIONS precedence doc; Q4 adds explicit red-toast failure mode for passphrase-no-agent; Q7 adds `tepegoz connect <alias>` CLI to 5d + 5b-to-5c gap doc note.
- **Q5 redirected from option (a) to option (b) tab-strip.** Spec-hierarchy drift — engineer cited DECISIONS + CLAUDE.md but not README mockup. Mockup shows PTY tile with visible multi-pane list; option (a) would have hidden non-active panes behind a `Ctrl-b w` overlay, regressing the "all activity visible at a glance" tagline. User explicitly endorsed: *"One line of chrome is not a cost — it's the point."* Neither (a) nor (b) is a Decision #7 amendment (layout ≠ content); tab strip sits above the vt100 render area inside the same Rect. Caught pre-implementation this time, unlike the Slice C mode-switch drift — process working.
- **Three user UX sign-offs baked into the relay:** (1) Fleet `procs` column is em-dash placeholder until Phase 6 fills it — no one-shot SSH probe workaround; (2) Fleet state markers are four-state (`●`/`◐`/`○`/`⚠`) not mockup's two; (3) PTY tab-strip uses `*` suffix + desaturation, NOT bright cyan (reserved for tile-focus per existing renderer contract).

### What's in flight with the engineer

**Slice 5d-ii** — TUI pane-stack + tab strip + keybinds + `tepegoz connect` CLI. Full scope in the 5d-ii relay (restated self-contained because engineer memory was cleared post-5d-i):

1. **Pane-stack data model** — `App.pane_stack: Vec<PaneId>` + `App.active_pane: usize`. Starts as `[local_root_pane_id]` at TUI startup. Opens push + move active. Close removes; auto-re-open local root if stack would empty.

2. **Tab strip inside PTY tile Rect** — 1-line strip at top of Rect per Q5 option (b) user-endorsed. Format `[1 local] [2 ssh:staging*] [3 ssh:dev-eu]`. Star suffix `*` on active; inactive labels `Modifier::DIM`. NOT bright cyan (reserved for tile-focus per existing scope-renderer contract). Overflow `[+N]` indicator when >9 panes. Decision #7 NOT amended — tab strip is chrome sitting above the vt100 render area inside the same Rect.

3. **Keybinds** — `Ctrl-b 0..9` jump, `Ctrl-b n/p` cycle (wrap), `Ctrl-b &` close-current (auto-reopens local root if would empty), `Ctrl-b Enter` on focused Fleet row dispatches `OpenPane { target: Remote { alias } }` + push + focus to PTY. `Ctrl-b w` explicitly unwired — deferred to 5e/v1.1.

4. **`tepegoz connect <alias>` CLI** — `Command::Connect { alias }` clap subcommand. Launches short-lived TUI, opens remote pane as initial action, stack contains only this remote pane. `Ctrl-b d` detaches + exits. Failure → stderr + non-zero exit.

5. **Integration test** — two remote panes opened, pane-stack contains both, AttachPane of stack[0] re-attaches to first pane with byte-exchange working both directions. Tab-strip visual via `ratatui::backend::TestBackend`.

6. **5e polish items** engineer may fold in naturally (otherwise defer): (a) FleetScope empty-list body-text branch on discovery-error source, (b) toast-gating comment-vs-implementation mismatch in 5c-ii (my preference: remove comment's "re-toast if reason changed" claim, accept state-only gating as v1).

### What I'm expecting next

- **Engineer's 5d-ii commit.** Watch closely on review: (a) tab-strip visual-hierarchy calls — desaturation level for inactive labels, column-width behavior under many-panes pressure, overflow indicator shape; (b) pane-stack lifecycle on TUI reconnect after detach — does daemon's `ListPanes` drive stack restoration, or does TUI lose stack state on detach showing only the last-attached pane? Your call; "lose on detach" is simpler for v1; (c) `tepegoz connect` failure-mode UX — does an unknown alias / connection failure print legibly to stderr? (d) integration test shape for TUI-driven pane-stack; (e) whether the 1-3s inline OpenPane block (flagged 5d-i concern) feels laggy in practice when user has active local pane + opens remote pane.
- **5e closes Phase 5** — error-surface polish + 8-scenario manual demo, same shape as Phase 4's close. Remaining 5e polish items (below). If the SSH-disconnect-kills-pane gap is painful to eyeball-test in the demo, may need a v1.1 stub in docs explaining "close the dead tab with `Ctrl-b &` and reconnect."

### Open questions I'm holding (not yet in DECISIONS.md)

- **Phase 5 load-bearing Q5 resolution is option (b) tab-strip.** Decision NOT amended. If 5d's tab-strip visuals turn out too noisy in practice (>4 panes at 120-col widths), the fallback polish is compacting labels to `[N]` without the name, OR reserving an overflow indicator `[+N more]`. Don't pre-optimize; let the 5d eyeball pass surface it if real.
- **5a follow-ups status (all five tracked from 5a review):**
  1. **ProxyJump surface** — deferred to 5c (`entry.proxy_jump.is_some()` pre-check + `SshError::ConnectFailed` with explicit v1 reason). 5b's doctor output already annotates `(not supported in v1 — Slice 5c surfaces this)`.
  2. **Tilde expansion** — ✅ done in 5b (`expand_tilde` + test `tepegoz_config_expands_tilde_in_identity_file`).
  3. **known_hosts 0600 mode** — ✅ done in 5b (PermissionsExt Unix-only + test `trust_sets_known_hosts_mode_to_0600`).
  4. **Agent certificate skip message** — ✅ done in 5b (explicit cert count in `AuthFailed.reason` attempts chain).
  5. **Include directive** — ✅ done in 5b. Engineer discovered russh-config raises `HostNotFound` on Include lines before any Host block, breaking parsing for real ssh_configs. Mitigation: `strip_include_directives` pre-strips Include lines before feeding russh-config. Test `include_directive_is_not_followed_phase_5_limitation` pins the behavior with self-documenting "if this starts failing, russh-config grew Include support — update OPERATIONS" comment. OPERATIONS.md documents the limitation.
- **5e polish items carried from prior slice reviews** (not blocking anything — captured so they don't get lost):
  1. **FleetScope empty-list body text** branches on source prefix. When `source.starts_with("discovery error:")`, swap "No SSH hosts configured — add entries to..." to "Host discovery failed — see source label above". Current copy blames the user's config for what is actually a parse failure.
  2. **`render_status_bar` hardcodes layout constants** (24, 30) in `short_source`. If the status-bar layout changes in 5d/5e, these silently truncate differently. Hoist to named constants or derive from other span widths.
  3. ~~**Orphan state-map entries in FleetScope.**~~ Absorbed into 5c-ii via coordinator-side `host_senders` HashMap diff on HostList refresh — supervisor tasks get dropped when aliases disappear. Closed.
  4. **Toast-gating comment/implementation mismatch (5c-ii).** Comment claims "re-toast if reason changed" but the gate is state-only (`prev_state != Some(state)`). Engineer picks Option A (implement reason-diff retoasting, track prev_reason) or Option B (remove the comment claim, accept state-only gating as v1). My preference: B.
  5. **Inline OpenPane blocking (5d-i).** Same-client commands serialize behind `state.remote_pty.open().await` for 1-3s on remote pane opens. May need spawn + request_id correlation if 5d-ii user-test shows this as laggy. Non-blocking unless observed.
  6. **`PtyManager::open` `debug_assert!(self.next_id < (1 << 32))` paranoia** (5d-i cheap add). Currently local + remote PaneId namespaces are disjoint by allocation-per-manager + 1<<32 offset; if local allocator ever hits 1<<32 (4 billion panes), namespace collision is possible. Not realistic for v1 but cheap to pin. 5e nice-to-have.
- **`--ssh-forget` decision resolved** — engineer picked Option A (land in 5b). Shipped in `f255681`. Docs + error + CLI are now consistent.
- **Phase 5 remote-pane session-persistence gap** (SSH flap kills pane) is a documented limitation. Lives in `docs/ISSUES.md` under a NEW section "Known limitations, Phase 6 upgrade path" per Q3 addition. Engineer creates the section in 5b or 5e; not an active bug.
- **Phase 4 polish candidates** (unchanged, tracked in STATUS):
  1. Help overlay for focus nav (`Ctrl-b ?` already reserved). User hit discoverability during Phase 4 demo scenario 3. Worth bundling into any Phase 5-adjacent TUI work OR landing as early v1.1 polish.
  2. Phase 3 carryovers: bounded `tail_lines` default (1000 with "load more"); logs-tile zoom if cramped; color palette feedback revisit.
- **OSC 0 title refresh on focus change** still stubbed. Even more useful now that tab-strip chrome in 5d will also need to update on active-pane change — may be natural to land both together.
- **Fleet `procs` column** shipping as em-dash in Phase 5, real values in Phase 6. If a user specifically flags the em-dash as confusing during 5e demo, revisit — but default is defer.

### Observations about engineer patterns (load-bearing for future direction)

- **Diagnostic discipline operates at its best when a bug is hard to narrow down.** Reference execution: Phase 4 wire-desync at `77aa9ca`. Shipped tracing in isolated commit (`bee6aba`), reproduced with targeted trigger (`yes | tui`), *falsified* a plausible hypothesis with an assertion (`debug_assert_eq` ruled out `AlignedVec` padding), identified actual root cause from byte-level log evidence, proposed minimal well-reasoned fix with symmetry argument, wrote regression test in default suite. This is the reference model.
- **Verifies fixes against the original production trigger, not just CI green.** Phase 4 fix: ran `yes | tui` for 20s alarm window on the fixed binary before declaring done. "Tests pass" and "bug fixed" are different claims; engineer checks both.
- **Catches real daemon bugs while trying to build tests.** C2 gate (`43b28eb`): discovered `pane_subs` leak because they refused to ship a vim-preservation test that didn't exercise the real `Unsubscribe` path.
- **Strong commit hygiene.** Messages capture *why* and blast-radius. Reference models: `43b28eb`, `c7b336d`, `4dd1208`, `77aa9ca`.
- **Good at salvage logic during pivots.** C1.5 tiling correction: explicitly enumerated what survives from C1/C2 and what gets deleted; updated docs in the same commit as the pivot.
- **Cross-OS CI discipline without prompting.** Caught the `printf \x1b` vs. `\033` POSIX gotcha via CI, not local-only testing. Platform divergence catches ship on every push.
- **Volunteers judgment calls at the right level.** Flags 3-5 tactical decisions per slice for review, doesn't flag naming noise. Matches `feedback_implementation_autonomy` model.
- **Adopts defensive testing patterns unprompted.** Examples: `push_toast_at(now, ...)` for time-travel tests; 2s sleep for status-counter-advance in restart round-trip; SIGSTOP-dockerd for timeout demo; python3 child + stdout-handshake for Phase 4 e2e (eliminates port-collision + bind-race flakes); `kill_on_drop(true)` instead of hand-rolled `ChildGuard`.
- **One watch-item:** occasionally needs the generalization-to-future-phases prompt. Examples where engineer's reasoning covered it (Slice D defer, desync fix class-of-bug), examples where I added it (the netstat2 deviation's Phase 7 implications). When signing off on deviations, explicitly ask "what does this lock for Phase N+1?"
- **Second watch-item: spec-hierarchy cross-check defaults to DECISIONS + CLAUDE.md, skips README mockup.** Phase 5 Q5 proposal argued (a) vs. (b) vs. (c) entirely against Decision #7's layout/content distinction — mechanically correct, but missed that the README mockup literally depicts a multi-pane PTY tile. Same failure class as the Slice C mode-switch drift. CTO caught it pre-implementation this time via the cross-check discipline, redirected to (b). When reviewing UX-adjacent proposals, the question "does the engineer cite the mockup?" is now a checklist item — if not, the CTO adds it explicitly. Memory file `feedback_cross_check_vision_before_signoff.md` covers this; the Q5 episode is a reinforcement, not a new lesson.
- **Strong flagging of escalation-worthy decisions.** Phase 5 proposal's "Flagged escalations" section correctly surfaced two items: Q5's Decision #7 claim (asking CTO to verify) and Q3's session-persistence gap (asking whether to pull Phase 6 forward). Both were the right calls to raise and the right calls to leave as-is. This is the "implementation autonomy within architectural guardrails" pattern working as intended — engineer makes the tactical call + explicitly flags the decisions that exceed tactical scope. Reference model for future proposals.
- **5a execution quality: reference tier.** Every directive from the Phase-5 sign-off relay landed exactly (Q4 passphrase-no-agent as verbatim attempts-list entry; Q2 precedence; TOFU location; `AuthFailed.reason` accumulating the full method chain — which I hadn't explicitly asked for but is the right shape for 5c's red toast). API surface is minimal and 5c/5d-consumable with zero "authenticate-then-do-thing" follow-up gymnastics. The `TofuOutcome` Arc-Mutex side-channel is the right trick for russh's constraint that Handler can't carry our structured error type. Tests cover the right edge cases without padding (ssh_config case-insensitive keyword, non-standard-port bracketing, missing-parent-dir creation). The five follow-ups in the 5b relay are genuine polish, not redirects. When the next slice's proposal feels tentative, remember 5a's shape — "concrete, minimal, all directives folded, no over-engineering, no under-engineering."
- **5b execution quality: reference tier, extended to CI recovery.** 5b itself folded all five 5a follow-ups correctly and caught a real russh-config bug (the `Include` directive issue that would have silently broken real users' configs — pinned by self-documenting test). Then when the Linux→darwin cross-build failed on push, engineer diagnosed via the CI run number, traced the regression to 5b's plumbing of tepegoz-ssh into consumer binaries (5a was isolated), chose ring with explicit feature-list matching modern-OpenSSH negotiation, and documented the rationale in Cargo.toml so a future performance-chaser doesn't flip it back. Then landed `--ssh-forget` per Option A with exactly the right "don't surgery hand-edited entries" security posture (first-token exact-match against tepegoz-written shapes only; multi-host + hashed + user edits all preserved by construction). This is the reference for how to close a slice that hits unexpected trouble: diagnose the actual cause, fix at the right layer, document so it doesn't regress.
- **Security-conscious default on destructive operations.** `KnownHostsStore::forget` instinctively chose the conservative blast-radius — only remove entries in the shape we wrote, preserve everything else. The memory entry `feedback_diagnose_before_fixing` has a cousin here worth naming eventually: for any operation that mutates user-visible state, the right default is "touch only what you created, preserve user authorship." If a similar choice comes up in 5c/5d/Phase-6 (e.g., remote-agent deploy replacing a user's `~/.local/bin` entry), the engineer's instinct is correct and should be echoed back rather than second-guessed.
- **Cross-session HANDOFF edit flow works.** CTO edits HANDOFF (from pre-clear or mid-review) land as uncommitted changes in the tree; engineer stages + commits them alongside their slice work. 5a carried my post-proposal-review HANDOFF edit this way. No coordination ceremony needed — the edit is just another uncommitted file in the working tree when engineer runs `git status` before committing.

### Standing context (if you're the fresh CTO reading cold)

- **Your role:** CTO / planner / architect, promoted by user "Emin" on 2026-04-13. You don't write code; the engineer does. Your job: proposal review, architectural sign-off, ordering of work, flagging product-level drift. You can edit `docs/`, `README.md`, and memory files — that's not "code." You cannot touch `crates/` or anything Rust.
- **Relay pattern:** user mediates between your session and the engineer's session. Engineer doesn't see your internal reasoning, only directives the user relays. Write engineer-facing messages as self-contained, unambiguous, ordered — they pick up cold from the relay. When user says "give me the full message and only the message," lead with the relay text marked for verbatim paste.
- **Spec hierarchy:** `README.md` + mockup first, `docs/DECISIONS.md` second, `docs/STATUS.md` / `docs/ROADMAP.md` / `docs/ARCHITECTURE.md` / `docs/OPERATIONS.md` / `docs/ISSUES.md` third. Check README before signing off on UX proposals (memory: `feedback_cross_check_vision_before_signoff.md`). The mode-switch-drift in Slice C happened because a previous-me skipped this check.
- **Seven locked architectural decisions** in `docs/DECISIONS.md`. Changing any requires user sign-off. #7 (tiled god view, opinionated default, vt100 via `vt100` crate) was added 2026-04-14 after the Slice C mode-switch drift.
- **Session-start ritual** in `CLAUDE.md`: read it + STATUS + ISSUES + HANDOFF, verify git log matches claims, fix stale entries before acting. Don't act on a memory or handoff entry if reality diverges — trust reality, update the docs.
- **Working memory:** `~/.claude/projects/-Users-emin-Documents-projects-personal/memory/`. `MEMORY.md` is the index. Current entries: user profile, sharpen-before-AI, diagnose-before-fixing, demonstrable-acceptance, implementation-autonomy, durable-project-state, session-boundary-handoff, cross-check-vision-before-signoff, project_tepegoz.
- **Phases shipped:** 0 (scaffold), 1 (proto + daemon + TUI), 2 (pty multiplex), 3 (Docker scope), 4 (Ports + Processes). Current: Phase 5 proposal pass pending.
- **Phases deferred:** Slice D (DockerExec) deferred to v1.1 — reopens when Phase 5 crystallizes the "scope → new pane" mechanism.
- **If the current state of the tree doesn't match this HANDOFF:** trust the tree, update HANDOFF. `git log --oneline -10` + `cat docs/STATUS.md` is the authoritative answer to "where are we?"

---

## Engineer section

**Last updated:** 2026-04-15, post-5d-ii push. Phase 5 closure awaiting 5e (error-surface polish + 8-scenario manual demo).

### Where I left off

**Slice 5d-ii landed.** Wire protocol unchanged from v9 (5d-i already added `PaneTarget` on `OpenPaneSpec`). TUI gains the full pane-stack machinery on top of 5d-i's daemon-side RemotePane:

1. **`App.pane_stack: Vec<PaneEntry>` + `App.active_pane: usize`**, replacing the single-pane `App.pane` + `App.pane_sub` + `App.pty_parser` fields. Each `PaneEntry` carries pane id, sub id, tab-strip label, and its own `vt100::Parser` — switching tabs restores the previous screen without a daemon re-subscribe. Event routing in `handle_daemon_envelope` now looks up the target entry via `find_pane_by_sub(subscription_id)` instead of equality against a single `pane_sub`; PaneOutput/PaneSnapshot bytes only emit `DrawFrame` when they hit the active pane's parser.
2. **Tab strip inside the PTY tile Rect.** 1 row of chrome at the top of the tile rendering `[N label]` entries with a `*` suffix on active, `Modifier::DIM` on inactive. Bright cyan is intentionally NOT used — reserved for tile-focus indication per the existing scope renderer contract. More than nine panes collapse into a `[+N]` overflow indicator. Decision #7 NOT amended: the strip is chrome sitting above the vt100 render area inside the same Rect. `pty_content_dims` helper carves the strip off `pty_tile_dims`; parser sizing + `ResizePane` send content dims, not tile dims, so vim/top render inside the box (integration-test pin: `pane_stack.rs`'s `two_panes_multiplex_bytes_on_independent_subscriptions`).
3. **`Ctrl-b` keybinds.** `Ctrl-b 0..=9` → `InputAction::PaneDigit(d)` → `pane_select_digit`; `Ctrl-b n/p` → cycle with wrap; `Ctrl-b &` → `close_active_pane` (ClosePane + Unsubscribe + auto-reopen local root if stack would empty); `Ctrl-b Enter` → `InputAction::OpenPaneAtSelection` → dispatches `OpenPane { target: Remote { alias } }` for the focused Fleet tile's selected row (no-op on any other focus). `Ctrl-b w` explicitly swallowed at the filter level so accidental presses don't leak `w` into the pty while the overlay is deferred (`docs/ISSUES.md#ctrl-b-w-pane-list-overlay-deferred`).
4. **FIFO correlation for PaneOpened / Error.** The wire has no per-request id on `OpenPane`, so `pending_opens: VecDeque<PendingOpen>` correlates by daemon-serial ordering. `handle_pane_opened` pops the head + pushes a new `PaneEntry` + sends `AttachPane`/`ResizePane` + focuses PTY. `handle_daemon_envelope`'s `Error` arm consumes the head of `pending_opens` first if non-empty, surfacing "open pane ssh:staging failed: {reason}" as a red toast; falls through to the generic daemon-error toast otherwise. Edge case documented: a post-open AttachPane failure could mis-consume a later PaneOpened's FIFO slot — accepted v1 risk, pinned for Phase 6 when agent-backed panes give us a proper request_id channel.
5. **PaneExit semantics.** On PaneExit for an entry: remove it. If stack empties → `Detach(PaneExited { exit_code })` (preserves the single-pane contract — `tepegoz connect <alias>` dying via SSH flap returns to outer shell). Otherwise clamp `active_pane` to a valid index and emit an Info toast "pane {label} exited (code {code})". Close initiated via `Ctrl-b &` eagerly removes + opens local root — never lets the PTY tile go blank.
6. **`tepegoz connect <alias>` CLI.** `Command::Connect { alias, --socket }` in clap; delegates to new `tepegoz_tui::run_connect(config, alias)` which shares `session::run`'s code path with two overrides: `PaneTarget::Remote { alias }` for the initial OpenPane + `reuse_alive: false` so it always creates a fresh remote pane instead of piggybacking on a leftover local one. Unknown alias / connect failure falls out of `ensure_pane` as `anyhow::bail!("open pane failed: …")` → stderr + non-zero exit, matching `doctor --ssh-forget`'s shape.
7. **5e polish items folded in.**
   - FleetScope empty-list body text now branches on `source.starts_with("discovery error:")` → "Host discovery failed — see source label above" (pinned by new render test `discovery_error_source_swaps_empty_list_hint`). Was previously blaming the user's config for a parse failure.
   - Toast-gating comment in `HostStateChanged` rewrite: removed the "re-toast if reason changed" exception claim, replaced with honest "state-only gating is v1; reason-diff retoasting is v1.1 polish" (per CTO Option B preference).

### Test coverage added

- TUI lib: +27 tests (166 → 193). Breakdown: `input` +6 (ctrl-b n/p/&/digit/enter + w-swallow); `pty_tile` +4 (labels + active star + overflow indicator + 10-pane cap); `app::tests` +16 (root label, next/prev wrap, digit select, digit-out-of-range, digit-zero-is-tenth, close-shifts-active, close-last-opens-local, close-off-pty-is-noop, enter-on-fleet-dispatches, enter-on-pty-noop, pane-opened-pushes-and-focuses, pane-exit-single-detaches, pane-exit-non-active-removes, error-during-pending-open, label-from-shell); `scope::fleet::tests` +1 (discovery-error hint).
- Integration: `crates/tepegoz-core/tests/pane_stack.rs` — `two_panes_multiplex_bytes_on_independent_subscriptions` opens two local panes, attaches distinct sub ids, asserts per-pane byte isolation (markers never cross-contaminate), closes pane A, asserts pane B keeps streaming + ListPanes reflects the state. Local panes stand in for remote panes: identical wire shape (only `OpenPaneSpec.target` differs), zero SSH setup needed in CI.

### What I'm mid-flight on

_Nothing — 5d-ii is pushed and CI green is my next gate._

### What I'm expecting from the CTO next

- **5d-ii review.** Load-bearing call-outs: (a) tab-strip visual hierarchy — chose `Modifier::DIM` + `Color::DarkGray` for inactive, `Modifier::BOLD` + terminal-default foreground for active (no bright cyan per existing contract); (b) pane-stack on detach + reattach — goes with the simpler "lose stack state on detach" policy; `tepegoz tui` reuses an alive pane if any via `ensure_pane`'s ListPanes path, otherwise opens a fresh local root. No daemon-side ListPanes-drives-stack-restoration yet; a future slice can add that. (c) `tepegoz connect <alias>` failure-mode UX — unknown alias / connect failure surfaces via `anyhow::bail!` from `ensure_pane`, which the binary's `?` propagation prints to stderr and returns non-zero; matches `tepegoz doctor --ssh-forget` error shape. (d) Integration test shape — chose always-on local-pane wire test over an opt-in remote variant to keep CI fast; the remote wire shape is covered by 5d-i's `remote_pane.rs::remote_pane_open_attach_exec_roundtrip`. (e) Inline OpenPane blocking (5d-i carryover) — not reopened; I don't have a user-test signal yet to justify the spawn + request_id correlation refactor.
- **5e** — error-surface polish + 8-scenario manual demo to close Phase 5. Items I'd suggest for 5e: update STATUS.md to document Phase 5 end-to-end (STATUS is currently stale from the Phase 4 close); update ROADMAP; STATUS's "What works end-to-end" section needs new bullets for Fleet + remote pane + tab strip + `tepegoz connect`.

### Anything that would surprise a fresh-me (5d-ii-era)

- **`pending_opens` FIFO correlation is brittle to AttachPane failures.** After a successful PaneOpened, the client sends AttachPane to start the stream. If THAT attach fails (unknown pane_id — shouldn't happen in practice, since we got PaneOpened in the same breath), the daemon's Error reply would consume the head of `pending_opens` even though that head belongs to a later in-flight open. Accepted risk for v1; reopen when the wire gets a per-OpenPane request_id.
- **Tab strip reserves the TOP row of the PTY tile Rect.** `pty_content_dims(layout) = (tile_rows - 1, tile_cols)`. Daemon-side `ResizePane` carries the content dims, not the tile dims — if you regress this, vim will render one row taller than the content area + the cursor will wrap inside the tab strip. Pinned by the updated `resize_recomputes_layout_and_sends_resizepane_with_pty_tile_dims` test + the new content-size assertion in `initial_actions_emit_attach_resize_with_pty_tile_dims_and_subscribe_docker`.
- **`MAX_TAB_DIGIT_SLOTS = 9` for visual slots; keybinds support 10 via digit 0.** CTO spec: first 9 numbered, 10th keybind-only, `[+N]` overflow kicks in at 10+. If you bump the visual cap, update the spec in `docs/ISSUES.md#ctrl-b-w-pane-list-overlay-deferred` too — the overlay is the proper long-term answer for >9 panes.
- **`pane_label_from_shell` is MAX 20 chars + `…`.** Long SSH aliases get truncated so the strip stays legible on 80-column terminals. Non-ASCII is fine because truncation uses char count, not byte count — but the slice operator on the path `str[..MAX_LABEL]` IS byte-indexed and can panic on multibyte boundaries. Defer to a char-indexed truncation if the user surfaces non-ASCII aliases as a real case; the 20 cap makes a byte-boundary panic almost impossible in practice for English/ASCII aliases.

### Anything that would surprise a fresh-me

**Load-bearing: `tokio::AsyncReadExt::read_exact` is not cancellation-safe.** Using it directly in a `tokio::select!` arm where other branches can fire will silently corrupt the reader stream when `read_exact` is interrupted mid-read. The pattern for the TUI is: dedicate a reader task, funnel envelopes through an mpsc, `select!` on `mpsc::Receiver::recv()` (which IS cancellation-safe). Same discipline as the daemon's dedicated writer task. If you're adding a new `select!` that reads from a stream, think cancellation safety FIRST. Classic failure mode: "works in tests, breaks under heavy stdin or winch pressure" — exactly how Phase 4's manual demo failed. Pinned by `session::tests::stdin_pressure_does_not_desync_large_envelopes`. Burned 4+ hours diagnosing Phase 4 4d; don't repeat the lesson.

**Load-bearing: `ProcessesProbe::sample(&mut self)` is stateful by design.** (Elevated from the 4b surprises list per CTO directive — mis-refactoring this silently kills CPU% in production, and tests would still pass.) sysinfo computes CPU% as a delta between consecutive `refresh()` calls, so the probe must persist across sampling cycles. The daemon's `forward_processes` task moves the probe into `spawn_blocking` each iteration and receives it back via the closure return tuple, keeping sysinfo's internal process map + CPU baseline alive while the tokio runtime stays unblocked. On `JoinError` (spawn_blocking panic), the task resets to a fresh probe — the next emit correctly carries `cpu_percent: None` for every row because we can't compute a delta across a crash boundary. **Do not refactor `sample` into a stateless free function or a `fn sample(&self)` method; tests would still pass but every sample would carry `None` and CPU% would be silently dead in production.**

4c-era items:

- **Selection persistence uses stable keys, not positional indices.** `PortKey { protocol, local_port, pid }` and `ProcessKey { pid, start_time_unix_secs }` are computed from the currently-selected row BEFORE a state transition, then used to re-anchor the cursor AFTER the transition via `reanchor_selection(old_key)`. Three invariants (all pinned by state-machine tests): (1) rows reorder → cursor follows the key to its new index; (2) selected entity disappears → cursor clamps into the new visible range (doesn't crash, doesn't stick on a removed row); (3) pid reuse under a different `start_time` never silently retargets. If you add filter-change handling, filter-narrowing, or similar state shifts, use `reanchor_selection` — don't hand-clamp.
- **`p` toggle is absorbed at the outer scope BEFORE filter/nav dispatch.** `handle_ports_key` checks `ScopeKey::Char(b'p')` at the top and only flips `active` if the current view's `filter_active` is false. While filter-typing, `p` falls through as a normal filter character. Without this carve-out, typing `postgres` into the filter would toggle views mid-word. Tested by `p_does_not_toggle_while_filter_is_active_on_ports_view`.
- **Render-layer em-dash is `"   —"`, not `"-"` or `"n/a"`.** The `cpu_text` match in `render_processes_table` emits a 4-char field starting with 3 spaces + the unicode em-dash (U+2014). Width matches the right-aligned `Some(x)` format (`"{:>5.1}"`). If the table columns ever change width, update both arms in lockstep — rendering tests pin em-dash presence (`rows.contains("—")`) but not exact column alignment.
- **`UDP coming v1.1` footer hint is an explicit-defer cue.** The 4c UDP resolution was option (c): TCP-only, user-visible indication. If you later ship UDP: remove the hint, flip the probe's `ProtocolFlags::TCP` to `ProtocolFlags::TCP | ProtocolFlags::UDP`, add a `protocol` column to the table (or keep it in the row header — it's already the first column), and add a render test that both TCP and UDP rows appear distinguishably.
- **Ports tile dispatch is a two-arm match, not a chain of `Some(X) = ...`.** In `handle_forward_bytes`, I converted the previous `if let Some(ScopeKind::Docker) = ...` to a `match` on `Some(Docker)` / `Some(Ports)` / `None`. This scales linearly — Phase 5 (`ScopeKind::Fleet`) adds one more arm. If scope count ever hits 6+, consider a method on `ScopeKind` that returns a `&dyn ScopeHandler`, but don't pre-optimize for now.

4b-era items:

- **`ProcessesProbe` is stateful; `list_ports()` is stateless.** Keep this as orientation even though the load-bearing warning above supersedes it. `list_ports()` is `fn() -> Result<Vec<ProbePort>>` (stateless). `ProcessesProbe::sample(&mut self) -> Result<Vec<ProbeProcess>>` (stateful). If you add a third probe kind, ask: is there cross-sample state (deltas, rate averages, cached sockets) → struct; otherwise → function.
- **`cpu_percent: Option<f32>` on the wire carries semantic meaning `f32` alone can't.** `None` = "first sample, sysinfo had no prior delta, TUI renders em-dash". `Some(0.0)` = "measured as idle". `Some(x)` = "measured as x%". Docker stats uses `f32` with `0.0` as sentinel for the same situation; Processes made a different call per CTO's explicit 4b note. Roundtrip test `process_list_event_roundtrip_preserves_first_sample_cpu_none` pins that `None` doesn't collapse to `Some(0.0)` through rkyv.
- **`forward_processes` uses move-into-spawn-blocking + return-back-via-tuple** to persist `ProcessesProbe` state across iterations while still running the sync refresh on tokio's blocking pool. On `JoinError` (probe task panicked), the task resets to a fresh probe; this means the NEXT emit after a panic will again carry `cpu_percent: None` for every row — intentional (can't compute a delta across a crash boundary), but worth knowing when debugging "why did CPU% disappear?" events.
- **macOS cmdline resolution is degraded.** sysinfo's `Process::cmd()` on macOS sometimes returns only `["sleep"]` when the real cmdline was `sleep 30`. The opt-in integration test asserts `command.contains("sleep")` not exact match for this reason. If 4c renders cmdlines and users complain about "truncated args," check sysinfo's macOS libproc backing and whether reads require different privileges.
- **`ChildGuard` pattern for test-spawned children.** If you spawn a child process in an integration test, wrap it in a `struct ChildGuard(std::process::Child)` with a Drop impl that calls `kill()` + `wait()`. Without it, a panic mid-test leaks the child into the test runner's parent shell. Use this pattern in every probe-ish test going forward (Phase 5 SSH tests, Phase 7 scanner tests).
- **sysinfo 0.31's `refresh_processes_specifics` takes 2 args, not 3.** Older sysinfo tutorials show `(processes_to_update, remove_dead, refresh_kind)` but 0.31 dropped the middle bool. First compile attempt will surface this — don't spend time on the error, just drop the bool.

4a-era items:

- **Linux correlates port → container in the probe (via cgroup); macOS correlates in the daemon (via bollard port match).** Not a mistake or asymmetry — macOS pids can't carry a cgroup reference (Docker Desktop runs containers inside a Linux VM, so macOS-visible pids are VM host pids, not in-container pids). The only workable correlation on macOS is `local_port` → `HostConfig.PortBindings`. Linux has both options; we picked cgroup because it needs no Docker engine connection. If you change this, flag it — the two-layer split is easy to miss.
- **`forward_ports` skips Docker entirely on Linux.** The whole correlation block is `#[cfg(target_os = "macos")]` inside `forward_ports`. This is an optimization: on Linux with non-containerized processes, `container_id == None` is the correct final answer (no container to correlate to), and without the cfg guard the daemon would do a pointless `Engine::connect` on every poll. If you ever need Linux-side Docker correlation for a different reason, remove the cfg guard BUT then avoid triggering it for non-containerized processes.
- **`netstat2` is the listening-socket backend, not raw netlink.** Deviates from the Phase 4 proposal. The Cargo.toml comment explains why; the wire contract didn't change so an upgrade to `NETLINK_SOCK_DIAG` can happen later without touching the daemon or clients.
- **`tokio::task::spawn_blocking` wraps every `list_ports` call.** The probe does filesystem reads + libc calls on macOS and is not async. Calling it directly from the tokio runtime would block whatever reactor thread the `forward_ports` task landed on. Mirror this pattern in 4b's processes probe — `sysinfo` has the same blocking shape.
- **`partial: bool` on ProbePort is the graceful-degradation signal.** When the probe sees a socket but can't attribute it (pid unknown, permission denied, process vanished mid-read), the row comes through with `pid == 0`, empty `process_name`, and `partial: true`. The TUI in 4c renders these with a visual cue (e.g., dimmed or with a `?` prefix) so users know to elevate. Non-partial rows have complete data; don't emit partial rows from newly-added fields without checking this invariant.
- **Pid reuse across a 2 s refresh is a Phase 4 risk.** Short-lived processes can get a pid that's later reused for another binary. For Ports this is rare (listening ports imply long-lived processes). For Processes (4b) it matters — CTO's selection-persistence note specifically called for `(pid, process_start_time)` tuples. sysinfo exposes `Process::start_time()`; use it for selection persistence, not just pid.

Phase-3-era items still relevant:

- **Capital-discipline rule on action keybinds.** Lowercase = safe / navigation (`r` restart, `s` stop, `j`/`k` navigate, `l` logs); CAPITAL = destructive (`K` kill, `X` remove). Capital `R` and capital `S` are explicit no-ops on the Docker list view — *not* case-insensitive aliases. Rationale: caps-lock can't silently escalate a safe action into something unexpected, and the "caps = stop-to-think" muscle memory stays consistent when Slice D / Phase 4+ add more keybinds. Pinned by the `capital_r_with_docker_focus_is_noop` test. Adding a new destructive action? Bind it to a capital. New safe one? Lowercase only.
- **K/X during an already-open confirm are ABSORBED, not cancels.** Pressing K then K again keeps the modal showing Kill — does NOT toggle off, does NOT switch to a second Kill with a fresh deadline, does NOT switch target. Same for X during Kill-pending or K during Remove-pending. Rationale: a second K/X is most likely "still intending to confirm" muscle memory; cancelling on repeat keypress would surprise the user. Contract: `y`/`Y` confirms, K/X absorb, anything else cancels. Pinned by `second_k_during_kill_pending_is_absorbed` + `x_during_kill_pending_is_absorbed`.
- **`MAX_LOG_LINES = 10_000` is the LogsView buffer cap.** Ring-style `VecDeque<LogLine>` with drop-oldest on overflow. Memory ~1–2 MiB for typical lines; higher for long-line content. Pinned by `max_log_lines_drops_oldest`. If scrollback-beyond-cap ever matters (e.g., a "load more" affordance), reopen with a higher cap or bolt on pagination — don't just raise the cap without bounding total memory.
- **`tail_lines: 0` on `Subscribe(DockerLogs)` means "all history"**, not "no history" — Slice B wire contract. On a chatty long-lived container pressing `l` dumps megabytes on entry. Flagged in `docs/STATUS.md` Phase 3 polish candidates; not fixed in C3. If you implement the `tail_lines: 1000` default with a "load more" affordance, the wire is already there — daemon honors whatever value the client sends.
- **The 2-second sleep in the restart round-trip test is load-bearing.** `crates/tepegoz-core/tests/docker_scope.rs::restart_propagates_to_follow_up_container_list` snapshots pre-restart `state`/`status`, sleeps 2 s so "Up N seconds" advances visibly, THEN sends Restart. Without the sleep, a fast-enough restart lands with the "Up 1 second" counter unchanged → the post-Success `ContainerList` looks identical to the pre-restart one, the assertion fires on a shift that never happened, and the test ships flaky. If you add a similar timer-dependent integration test (Phase 4 Processes CPU% sampling has analogous dynamics), bake in the deliberate pre-event sleep rather than trusting the post-event timing to be distinguishable.

Carried forward from Slice C3 as still relevant:

- **`push_toast` has a `push_toast_at(now, ...)` variant for the sweep.** Toast `expires_at` is computed from an explicit `Instant` rather than always `Instant::now()` so the `sweep_expired(now)` code path doesn't evict a freshly-pushed timeout toast in the same pass. Tests pass synthetic "31 s in the future" nows to simulate time travel.
- **The `next_sub_id` allocator is shared across subscription ids and DockerAction request ids and the per-container DockerLogs sub id.** The daemon correlates by id embedded in the payload, so collision between namespaces is a non-issue. One monotonic counter keeps everything simple.
- **Focus-away cancellation fires *before* the focus mutation** in `handle_focus_direction`, so the old focus (`TileId::Docker`) is still observable when we clear `pending_confirm`. Order matters.
- **Confirm modal takes priority in `handle_scope_key` over the filter branch.** Logs view takes priority over *both* — if `view == Logs`, `handle_logs_key` runs and returns before confirm/filter are even checked. Handler order is the right defense even if these states can't actually coexist today.
- **Toast rendering lives in `toast.rs`, not `session.rs`.** Pulled into its own module so render tests verify overlay positioning against a real `TileLayout` without dragging the full runtime in. Same pattern should apply to Phase 4/5/7 scope panels.
- **`LogsView::container_id` is `#[allow(dead_code)]`-marked.** The renderer displays `container_name` instead (shorter + readable); the id is kept for tests and any future "reopen logs after reconnect" flow. Fully intentional.
- **CRLF handling in `LogsView::ingest`.** A trailing `\r` before the `\n` is stripped along with the `\n` so Windows-container logs render cleanly. Tested (`crlf_terminated_lines_strip_both_bytes`).
